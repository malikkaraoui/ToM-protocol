[package]
name = "tom-metrics"
version = "0.1.0"
edition = "2021"
description = "Minimal metrics primitives for the ToM protocol stack"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
