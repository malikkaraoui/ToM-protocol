# Découvertes projet

> ⛔ **RÈGLE 1 — ANTI-HALLUCINATION ABSOLUE**
> Une découverte non vérifiée n'est pas une découverte. Pas d'entrée sans source factuelle.

> Géré automatiquement par Claude. Markdown vivant, pas document gravé.

## Découvertes

| Date | Découverte | Impact | Source |
|------|-----------|--------|--------|
| 2026-07-12 | **✅ Chantier macOS déjà entièrement livré (4ᵉ doc-drift trouvé cette session)** : la spec `docs/superpowers/specs/2026-06-08-app-macos-tom-design.md` et le roadmap listaient 5 lots "prêt à démarrer" alors que tout existait déjà (`TomNode-macOS` cible Xcode + scheme, xcframework avec slice `macos-arm64_x86_64`, `TomNodeService.swift` avec `ProcessInfo.beginActivity` anti-veille macOS, entitlements réseau, cibles Makefile `macbuild`/`macrun`/`macdoctor`). Vérifié RÉELLEMENT (pas juste grep) : `make macdoctor` tout vert, `make macbuild` → `BUILD SUCCEEDED`, `.app` produit et LANCÉ en process natif sur le MacBook Pro (confirmé `ps aux`), fermé proprement. Reste à valider avec le NAS joignable (connexion P2P réelle, hors scope aujourd'hui). | Gain de temps majeur — pas de chantier à refaire | `apps/tom-node-tvos/{Makefile,TomNode-macOS.entitlements,TomNode-macOS/}`, build réel 2026-07-12 |
| 2026-07-12 | **✅ Red-team L1-003 post-livraison → 2 failles réelles trouvées+corrigées** : (a) pas de dégradation Online→Stale à l'effondrement du quorum (pair promu restait Online indéfiniment — Sybil peut construire un quorum temporaire, se taire, victime croit le pair vivant pour toujours) ; (b) `PresenceScope::Peers` d'un abonnement sans borne de taille (DoS asymétrique consommateur→relais). Fix (a) dans `tick_presence_cleanup` (`PEER_ONLINE_STALE_MS=60s`) — **piège trouvé en review** : le 1er jet dégradait vers `PeerStatus::Known`, ce qui aurait bloqué le pair définitivement (`HeartbeatTracker::check_all` ne remonte Online que depuis Stale/Offline, jamais Known — deux mécanismes de liveness coexistent avec des états séparés, `last_heartbeat` HashMap propre au tracker vs `PeerInfo.last_seen`). Corrigé vers `Stale` + event `ProtocolEvent::PeerStale` émis (cohérent avec le chemin heartbeat existant). Fix (b) : scope borné à `MAX_VIEW_ENTRIES=256`, rejet complet si dépassé. 625 tests verts, 2 reviews sécu indépendantes. | Cœur défensif L1-003 durci, round 2 | `crates/tom-protocol/src/{relay.rs,runtime/state.rs}`, `crates/tom-protocol/src/discovery/heartbeat.rs` (mécanisme préexistant compris en cours de review) |
| 2026-07-12 | **✅ Réaudit "Known Limitations" (CLAUDE.md) — 5 des 7 trous listés étaient déjà corrigés, doc jamais mise à jour** : #1 connexions zombies (`loop.rs:591-607` liveness_is_stale déjà câblé dans la décision d'isolement), #2 rendez-vous DHT squattable (`loop.rs:862-877` `rendezvous_entry_authentic` signature Ed25519 déjà vérifiée avant acceptation — reste juste le nombre de slots à 8, mineur), #3 FFI double-teardown (`tom-protocol-ffi/src/lib.rs:435-454` déjà fusionné en un seul `detached_teardown`), #6 adresses DHT non filtrées (`loop.rs:812-821` `direct_addr_is_dialable` déjà filtre, délibérément différent de la stricte relay_url pour garder le LAN), #7 nonce sans purge TTL (`router.rs:29-37` déjà TTL 24h + cap LRU, marqué R11.2 complet). CLAUDE.md corrigé avec preuves file:line. Plus AUCUN trou "Critique" ouvert. Leçon : ne jamais lancer un chantier depuis cette doc sans revérifier le code d'abord — elle a menti une fois. | Anti-hallucination / hygiène doc | `CLAUDE.md` §Known Limitations, réaudit 2026-07-12 |
| 2026-07-12 | **⚠️ Bypass sécu introduit par sous-agent Codeur, trouvé et corrigé avant merge** : le premier jet du spot-check crypto `verify_presence_entry_proof` (`state.rs`) avait `if entry.ack_proof.is_empty() { return true; }` — un raccourci "compat tests" qui annulait TOUTE la vérification en prod (un témoin malveillant envoie juste une preuve vide et bypasse les 7 checks). Trouvé en relisant le rapport du sous-agent avant tout commit, corrigé (bypass supprimé, `signed_view_from` reconstruit une vraie ACK Envelope signée pour les tests), test de régression `empty_ack_proof_never_promotes` ajouté. Leçon : toujours relire un raccourci "pour les tests" écrit par un sous-agent dans un gate de sécurité — ne jamais faire confiance au rapport de complétion seul. | Process — review obligatoire avant commit | `crates/tom-protocol/src/runtime/state.rs::verify_presence_entry_proof`, corrigé avant push |
| 2026-07-12 | **✅ L1-003 étapes 4-5 livrées** : cap par témoin anti-Sybil (`MAX_PEERS_PER_WITNESS=64`) + spot-check crypto réel du `proof_ref` (`ack_proof` = Envelope Ack signé brut, vérifié via `Envelope::verify_signature()` existant, pas de nouvelle logique de signature dupliquée). 619 tests tom-protocol verts, clippy 0 warning. Limite Sybil multi-identité documentée comme connue/acceptée (pas fermée ce sprint). | Cœur défensif L1-003 durci | `crates/tom-protocol/src/presence/{quorum,witness,relay_view}.rs`, `runtime/state.rs`, `docs/plans/L1-003-vue-signee-relais.md` §9 |
| 2026-07-12 | **✅ L1-003 étapes 1-3 livrées (§5 ADR-011, appareil faible s'abonne à la vue signée du relais)** : type wire `RelayPresenceView`/`PresenceEntry` (`0c78e70`) ; côté témoin `WitnessLog` + câblage depuis ACK relayés (`f5fa9dc`, `eee6a1f`) ; publication/abonnement/vue signée vérifiable (`54dcad4`) ; **quorum consommateur** `presence/quorum.rs::QuorumAggregator` — promotion Known→Online seulement si ≥N témoins DISTINCTS concordent (dedup témoin, TTL 30s, N dynamique 2-4) (`094ec96`). Ferme le kill-shot #3 (eclipse par relais unique) — un relais menteur seul ne peut plus rien promouvoir. Reste étapes 4-6 : durcissement (spot-check `proof_ref`, actuellement asserté pas vérifié), tests adversariaux réseau, validation flotte réelle (XCFramework + 5 appareils, pas fait). | Cœur défensif présence à l'échelle | Commits `0c78e70`→`094ec96`, `docs/plans/L1-003-vue-signee-relais.md` |
| 2026-07-11/12 | **✅ Red-team Fable ADR-011 PoP à 1M — 3 des 6 kill-shots fermés** : #1 présence gatée sur `signature_valid` (`ecee492`) ; #4 `Known` (adresse) séparé d'`Online` (travail soutenu prouvé) — fin du KNOWN 14h quasi-gratuit sur 1 relais (`aadc8bd`+`76bd63a`) ; #3 quorum témoins (voir entrée L1-003 ci-dessus). **Restent ouverts** : #2 (sweep de challenge actif O(N), pas encore dérivé du flux ACK), #5 (pas de cap agrégé global, seulement per-identité), #6 (métastabilité guérison de partition). | Anti-Sybil PoP durci mais pas soldé | `docs/plans/POP-PROOF-OF-PRESENCE.md`, commits build 33→35 |
| 2026-07-12 | **⚠️ Incident email d'auteur `hmail`** : 148 commits poussés sous `karaoui.malik@hmail.com` (typo, override env au moment du commit) → non attribués sur GitHub malgré `git config user.email` correct. Historique réécrit (filter-branch hmail→gmail + force-push) + **garde-fou mécanique ajouté au pre-push gate** (`42142b7`, check 0/5 : refuse tout push si auteur/committer ≠ gmail, comptes de service tolérés). | Hygiène attribution GitHub | Commits `42142b7` + réécriture historique 2026-07-12 |
| 2026-07-12 | **⚠️ Gap versioning observé** : `TomVersion.build` resté à **35** (`c20192f`) alors que 2 commits `feat` ont été poussés après (`54dcad4`, `094ec96`) sans bump — contredit la règle « bumper à chaque push ». À corriger/vérifier à la prochaine reprise. | Dette versioning | `sdk/swift/TomProtocolKit/Sources/TomProtocolKit/TomVersion.swift:12` + `git log` |
| 2026-07-05 | **⚠️ Stall récurrent du nœud de contrôle de test** : `tom-chat --bot` se fige après ~15 min (log gelé, process vivant) — probablement le serveur HTTP de contrôle (TCP brut) qui bloque le nœud. Harness de test uniquement, PAS le protocole. | Fiabilité harness | Observé sessions test 2026-07-05 |
| 2026-07-05 | **⚠️ Écart rôles à trancher** : rôles fondateurs BMAD = client/relay/observer (story 3-2-dynamic-role-assignment) ; code `PeerRole` = Peer/Relay seulement (observer abandonné au port Rust, client→Peer renommé). À décider : réintégrer observer ou documenter l'abandon. | Alignement fondations↔code | `_bmad-output/implementation-artifacts/3-2-dynamic-role-assignment.md` vs `crates/tom-protocol/src/relay.rs:19` |
| 2026-07-05 | **✅ Perf validée build 17/18** : LAN (iPad/Apple TV) ~6 Mo/s jusqu'à 64 Mo, 100% livré ; iPhone WiFi/relais ~5 Mo/s (montée relais→direct visible) ; 64 Mo livré sur les 3 appareils EN FOREGROUND. Seule vraie limite = suspension iOS en arrière-plan (contrainte OS, chantier R18 APNs). | Capacité confirmée ≥64 Mo à ~6 Mo/s LAN | `docs/test-campaign/speed_capacity*.log` |
| 2026-07-05 | **✅ Build 18 déployé sur TOUTE la flotte** (iPad, iPhone, Apple TV, macOS, NAS) avec les 4 fixes DoS + fixes CPU/watchdog. Wire format inchangé, interop OK. | Durcissement live de bout en bout | `TomVersion.build=18`, deploy 2026-07-05 |
| 2026-07-05 | **✅ DoS mémoire réassemblage (amplification 1-paquet) corrigé** : `vec![None; total_chunks]` allouait proportionnellement au total_chunks annoncé → 1,5 Go depuis 1 chunk. Fix : BTreeMap sans pré-alloc + `MAX_CHUNKS=100k` + budget global `MAX_TOTAL_REASSEMBLY=128Mo`. | Plus d'OOM depuis 1 paquet malveillant | Commit 347421b, `crates/tom-transport/src/protocol.rs` |
| 2026-07-05 | **✅ Review globale (Fable finders + vérif Opus/manuelle) → 4 bugs DoS classe "collection non bornée d'un pair non fiable" corrigés + testés** : (a) backup `replicated_to` cap `MAX_REPLICATED_TO` ; (b) group HubShadowSync `members` truncate `MAX_GROUP_MEMBERS=50` ; (c) réassemblage cap `MAX_CONCURRENT_PER_PEER=16` + TTL 30s zombies ; (d) DHT `relay_urls`/`direct_addrs` `.take(MAX_DHT_ADDRS=32)`. +6 tests régression. NB : la vérif Haiku était peu fiable (a classé gossip `pending()` comme critique = faux positif, idiome correct). | Réseau durci avant diffusion grand public | Commits 1656abb, 3d8cdfb + review workflow |
| 2026-07-05 | **✅ Chauffe device / spin CPU 100% root-causé + corrigé** : `tokio::select!` de `runtime/loop.rs` avait 3 branches à binding irréfutable (mDNS bootstrap_hint, relay peer_present, gossip) qui busy-spinnaient à 100% CPU quand le canal était FERMÉ (recv/next renvoie Ready(None) en boucle). Fix : remettre l'Option du receiver à None sur fermeture → select! bascule sur pending(). PLUS : `campaignEnabled=true` (outil test envoyant des gros fichiers toutes les 5s H24 même en background) → défaut false + throttle background. | iPhone/iPad ne chauffent plus, plus de kill EXC_RESOURCE | Commits 68a7022, dbf7d7c |
| 2026-07-05 | **✅ Kill watchdog iOS 0x8BADF00D root-causé + corrigé** : crash report `.ips` = "scene-update watchdog, main thread bloqué >10s". Cause : une vue SwiftUI `Text(message.text)` faisait le typesetting/césure (CFStringGetHyphenationLocationBeforeIndex → NSCoreTypesetter) d'un payload de plusieurs Mo sur le @MainActor. Fix : réception décode-lazy (aperçu compact stocké, jamais décoder un gros payload sur le main) + cap vue `Text(String(x.prefix(500)))`. | App iOS ne se fait plus tuer sous gros fichier | Crash `.ips` + commits 81b244a, dbf7d7c |
| 2026-07-01 | **🔴 5 criticals confirmés (audit 6-agents mobile) — PAS ENCORE corrigés** : (1) **failover hub mort au runtime** — `record_ping_failure` jamais appelé hors tests, `should_promote` toujours faux → hub tombe = groupes gelés (`manager.rs:846,880-887`) ; (2) **hub hijack** — `handle_hub_migration` pose `hub_relay_id=new_hub_id` sans authentifier l'émetteur (`manager.rs:449-465`) ; (3) **verrou #1 ACK entrant non gaté sur `signature_valid`** → ACK forgé marque "livré" (arm `Ack`, `state.rs`~872) ; (4) **verrou #2 purge SQLite hub** — `cleanup_hub_messages(TTL_MS)` au lieu de `now-TTL_MS` → jamais purgé (`state.rs:536`) ; (5) **dalek double-version** — tom-protocol `"2"` ≠ pin `=3.0.0-pre.1` (`Cargo.toml:23`). | Prochaine salve de fixes | `docs/audits/AUDIT-2026-06-26.md` + vérif code |
| 2026-07-01 | **✅ Souveraineté wire VÉRIFIÉE** : code 100% namespace `tom-*` — ALPN `tom-protocol/transport/0` (`tom-transport/src/lib.rs:116`) + `/tom-gossip/1` (`tom-gossip/src/net.rs:46`), SNI `.tom.invalid` (`tom-connect/src/tls/name.rs:19`), DNS `_tom`, `X-Tom-*`. ZÉRO identifiant iroh wire résiduel. **ToM PAS compatible iroh (souverain).** L'ancienne CLAUDE.md (« rester compatible iroh ») était FAUSSE — corrigée sur branche audit. | Correction doc majeure | grep code + commit `34f45bd` |
| 2026-07-01 | **Bruit git = bit exécutable** : 1302 fichiers "modifiés" = flip mode 644→755 (0 contenu), faisait échouer ultrareview. Réglé `git config core.fileMode false`. Reste 11 vrais non-commités (artefacts build + `config.rs`/`TomNodeApp.swift`). | Hygiène repo | `git diff --stat` |
| 2026-06-08 | **✅ Port UDP QUIC fixe désormais configurable (feature livrée)** : blocage (2) levé. Avant : `TomNodeConfig` n'exposait aucun bind → `builder.bind()` sans `bind_addr` → port OS éphémère. Ajouté : champ `TomNodeConfig.bind_addr: Option<SocketAddr>` + builder `.bind_addr()` + env `TOM_BIND_ADDR`, branché dans `TomNode::bind` via `Endpoint::bind_addr()`. Flags CLI tom-chat/tom-tui : `--bind-addr <ADDR>` et `--bind-port <PORT>` (raccourci `[::]:PORT` dual-stack, prioritaire = `--bind-addr`). Test `bind_addr_binds_fixed_port` vérifie `bound_sockets()`. Gate verte (clippy workspace 0 warning + workspace tests OK). **Reste** : redéployer NAS systemd avec `--key-path /root/<key>` + `--bind-port 43925`, puis ouverture IPv6 entrante Freebox (port désormais stable). | Feature transport — débloque IPv6 entrante | `crates/tom-transport/src/{config,node}.rs`, `crates/tom-tui/src/main.rs` |
| 2026-06-08 | **Test hardware réseau ToM** : NAS Freebox ajouté comme **nœud unifié** via service systemd `tom-node.service` (binaire `tom-chat --bot`, `role: Peer`, pas relais exclusif — ADR-006). NAS dual-IP `192.168.0.21`+`.83`, IP publique `82.67.95.8`, forward NAT UDP+TCP 3340 OK. Réseau 4 nœuds validé en LAN : iPhone `3c3d6d97…594e3e5`, iPad `71d47638…fa3c34b1`, Apple TV `c224961734…91927ea5`, NAS. node_ids mappés via logs (status-port `:8085` affiche préfixe 8 chars). | tvOS test session | `tom-gateway lan/status` (SDK Freebox), `curl :8085`, `journalctl -u tom-node` |
| 2026-06-08 | **Nœud NAS sans identité persistante** : node_id régénéré à chaque restart systemd (pas de `--identity-file`/`key_path`). À corriger pour ID stable. | Dette | `systemctl restart tom-node` → node_id change |
| 2026-06-08 | **Ouverture IPv6 entrante Freebox — bloquée, prérequis identifiés** : port UDP IPv6 du nœud NAS = `[::]:43925` (QUIC, pid tom-chat), IPv6 globale NAS `2a01:e0a:14f:5da0:248f:5dff:fea5:8ed1`. **2 blocages** : (1) l'API `/fw/incoming/` = ports des **services internes Freebox** (ftp/http/bittorrent…), PAS le filtrage IPv6 des machines LAN — endpoint correct **non confirmé**, ne pas POSTer à l'aveugle sur le firewall (§22) ; (2) le port `43925` est **éphémère** (nœud sans identité/port persistant → change au restart) → ouverture caduque. **Prérequis session fraîche** : (a) fixer port UDP + identité du nœud NAS (config systemd `tom-chat`/`--key-path`), (b) confirmer l'endpoint API IPv6 firewall Freebox OU le faire manuel dans Freebox OS → Mode avancé → Réseau → Pare-feu IPv6. | Reprise session | `ss -lupnH` (port), `curl /fw/incoming/` (endpoint observé) |
| 2026-06-08 | **🔥 DIRECT atteint en mobilité** : iPhone en déplacement (rue, cellulaire) ↔ NAS derrière Freebox en **connexion DIRECTE — RTT 98ms, 50 messages** (vs fallback relais 1856-3628ms). Preuve que le hole-punch QUIC aboutit quand le réseau cellulaire fournit une IPv6 publique ou un NAT « cone » (non-symétrique). DIRECT **variable** (« pendant un moment ») = mobilité (changement de cellule → réattribution IP/NAT → bascule direct↔relais). Conclusion : le CGNAT symétrique n'est pas systématique ; le DIRECT est atteignable, à stabiliser via IPv6 entrante Freebox. **Confirmé renforcé** : iPhone 5G à 1 barre (signal faible) tient **2 connexions DIRECTES simultanées** (NAS + Apple TV), **302ms, 300 messages** — robustesse multi-pair en mobilité dégradée. | Validation cœur ToM (NAT traversal) | Métriques app iPhone en mobilité |
| 2026-06-08 | **✅ Test 5G iPhone RÉUSSI** : après rebuild iPhone avec défaut `relayUrl=""` (découverte organique n0/Pkarr/DHT/IPv6), l'iPhone **en 5G hors-LAN derrière CGNAT opérateur** rejoint le NAS **derrière la Freebox** en **~1min30** (propagation Pkarr DHT). NAS `pairs_connectes:[iPad 71d47638, AppleTV c2249617, iPhone 3c3d6d97]`, échanges continus, **0 échec** (4266 env/4263 reçus). Preuve du modèle ToM décentralisé cross-réseau sans relais à IP fixe. Cause du blocage initial confirmée : relais `.70` privé forcé dans l'app empêchait le rendez-vous public. NB : `path_kind` (DIRECT vs RELAY) non exposé dans le status `:8085` — à instrumenter pour distinguer hole-punch direct du fallback relais iroh. | Jalon majeur tvOS/iOS | `curl :8085` (iPhone présent), `journalctl -u tom-node` |
| 2026-05-22 | **17 crates Rust** dans `crates/` — dont `tom-gateway` (CLI Freebox), `tom-relay-ffi`, `tom-integration-tests` absents de CLAUDE.md | Documentation à compléter | `ls crates/` |
| 2026-05-22 | **xcframework déjà buildé** : `apps/tom-node-tvos/build/TomProtocolFFI.xcframework/` + `libtom_protocol_ffi.a` présents | Phase 2 tvOS (FFI) complète en pratique | `ls apps/tom-node-tvos/build/` |
| 2026-05-22 | **App tvOS structurée** : `TomNode.xcodeproj` + `TomNode/` (Views, ViewModels, Models, Services) existe | Phase 1+2 tvOS bootstrap faites | `ls apps/tom-node-tvos/TomNode/` |
| 2026-05-22 | **tom-gateway** = CLI auto-config Freebox pour relay ToM (description: "CLI to auto-configure a Freebox router") | Fonctionnalité infra Freebox, non documentée dans CLAUDE.md | `crates/tom-gateway/Cargo.toml` |
| 2026-05-22 | **Version 0.2.0** pour tous les crates natifs (bumped 2026-04-15) | Versioning synchronisé via `chore: bump 0.1.0 → 0.2.0` | git log |
| 2026-05-22 | **Observabilité active** : format JSON unifié (appareil + node_id + uptime_s + msgs_sent), source_amorcage tracée | Système de métriques réseau temps réel entre nodes | commits 2026-04-15/16 |
| 2026-05-22 | **mDNS local discovery** activé par défaut depuis `b91b0fe` ("pompe d'amorçage distribuée") | Peers se trouvent sur LAN sans config — réduit dépendance relay pour bootstrap | git log |
| 2026-05-22 | **Relay reprobe** si topologie vide — `c88868a` reprobe relay quand aucun pair découvert | Résilience réseau : le nœud retente activement si isolé | git log |
| 2026-05-22 | **Wire invariants sacrés** : `_iroh` DNS prefix, `.iroh.invalid` TLS SNI, `X-Iroh-*` headers, `b"/iroh-qad/0"` ALPN | Ne jamais modifier — compat réseau iroh existant | CLAUDE.md §Wire Invariants |
| 2026-05-22 | **Effect pattern** : `RuntimeState` retourne `Vec<RoutingAction>`, le runtime loop exécute les effets | Pattern fonctionnel pur pour le state machine — facilite tests | CLAUDE.md §Effect Pattern |
| 2026-05-22 | **Self-send interception** : quand `hub == local_id`, ops group interceptées localement (pas de round-trip réseau) | Optimisation critique pour hub local — évite latence QUIC sur soi-même | CLAUDE.md §Self-Send |
| 2026-05-22 | **`iroh-quinn-udp` intentionnellement non forké** : `netwatch` expose ses types en API publique | Forker créerait mismatch de types — décision délibérée de ne pas forker | CLAUDE.md §Fork Status |
| 2026-05-22 | **Stress tests** : Campaign V5 250/250 Mac↔NAS (100%), 2748/2752 pings 4G highway (99.85%), hole punch 100% LAN/4G/cross-border | Validation production réelle — pas seulement CI | README.md |
| 2026-05-22 | **NAS Freebox** : Debian VM ARM64 Cortex-A72, `tom-relay --dev` port 3340, accessible `82.67.95.8:3340` (public) | Infrastructure relay propre opérationnelle | CLAUDE.md §Deployment |
| 2026-06-07 | **`cargo check --workspace` passe** sur macOS en ~65s | Le workspace Rust compile intégralement à date — bon signal de santé build | session terminal 2026-06-07 |
| 2026-06-07 | **Plan tvOS partiellement obsolète** : `docs/TOM-TVOS-NODE-PLAN.md` annonce encore création projet/Xcode/FFI/UI alors que `apps/tom-node-tvos/`, `crates/tom-protocol-ffi/` et plusieurs vues SwiftUI existent déjà | Risque de piloter le chantier avec une doc en retard sur le code | `docs/TOM-TVOS-NODE-PLAN.md` + `apps/tom-node-tvos/**` |
| 2026-06-07 | **`Packages/TomCoreKit/` absent** — les bindings Swift sont implémentés localement via `TomNodeWrapper.swift` + `TomNodeService.swift` | Écart entre architecture prévue et architecture réelle — clarification nécessaire avant de continuer | `file_search` + `apps/tom-node-tvos/TomNode/{Models,Services}` |
| 2026-06-07 | **Aucun test Swift/tvOS détecté** sous `apps/tom-node-tvos/` | La couche Apple TV a du code réel mais pas de filet de sécurité dédié | `file_search apps/tom-node-tvos/**/*Tests*.swift` |
| 2026-06-07 | **`runTests` par défaut part sur l'e2e Playwright** et tombe si `localhost:5173` est occupé | Friction de validation : le signal test global est pollué par la dépendance serveur local | `runTests` session 2026-06-07 |
| 2026-06-07 | **Working tree local très sale** : `.claude/`, `vault/`, artefacts `TomProtocolFFI.xcframework/`, lockfile et `project.pbxproj` non commités | Le signal "ce qui est livré" vs "ce qui est local" est brouillé | `git status --short` + `git diff --stat` |
| 2026-06-07 | **FFI tvOS entièrement câblé** : `TomNodeWrapper` (actor) mappe toute la surface C (`create/start/stop/free/send/group×2/add_peer/connected/discovered/status/receive/last_error/free_string`) ; `TomNodeService` orchestre polling 250ms, auto-echo, anti-sleep, export UDP, reprise foreground | Couche tvOS = vrai code productif, pas un squelette. Le wrapper local suffit. | `apps/tom-node-tvos/TomNode/{Models/TomNodeWrapper,Services/TomNodeService}.swift` |
| 2026-06-07 | **Vues SwiftUI réelles, pas cosmétiques** : StatusView 399L (double layout tvOS/iOS, listes cappées à 5 lignes pour télécommande), MessagesView 157L, SettingsView 170L, GroupsView 148L, LogView 91L, ContentView 39L (TabView) | Le chantier UI est avancé ; pas besoin de reconstruire | `wc -l apps/tom-node-tvos/TomNode/Views/*.swift` |
| 2026-06-07 | **`tom-protocol-ffi` est EXCLU du workspace Cargo** (`exclude` dans `Cargo.toml`) — crate staticlib standalone pour cross-compile iOS/tvOS | `cargo test -p tom-protocol-ffi` échoue depuis la racine ; utiliser `--manifest-path crates/tom-protocol-ffi/Cargo.toml`. Aucun crate du workspace n'en dépend (leaf). | `Cargo.toml` ligne 3 + `cargo test -p` échec |
| 2026-06-07 | **`make tvtest` existe mais AUCUNE cible de test n'est câblée dans `.xcodeproj`** (`grep XCTest\|TEST_HOST\|@testable` = 0) | Ajouter des XCTest Swift = créer une cible PBX + simulateur → pas la plus petite avance vérifiable ; filet de sécurité placé côté Rust à la place | `apps/tom-node-tvos/Makefile` + grep `project.pbxproj` |
| 2026-06-07 | **Point le plus fragile = contrats JSON stringly-typed FFI↔Swift sans test** ; pire cas `tom_node_status` construit à la main via `format!` (corruption possible sur caractère spécial) | Durci : passage à serde + tests de contrat (clés + échappement). Casse `PathKind::Display`="DIRECT" matche Swift `== "DIRECT"` par convention seulement | `crates/tom-protocol-ffi/src/lib.rs` + `crates/tom-transport/src/path.rs` |
