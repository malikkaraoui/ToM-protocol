# Découvertes projet

> ⛔ **RÈGLE 1 — ANTI-HALLUCINATION ABSOLUE**
> Une découverte non vérifiée n'est pas une découverte. Pas d'entrée sans source factuelle.

> Géré automatiquement par Claude. Markdown vivant, pas document gravé.

## Découvertes

| Date | Découverte | Impact | Source |
|------|-----------|--------|--------|
| 2026-07-18 nuit | **📖 RUNBOOK DE TEST créé (`docs/plans/RUNBOOK-TESTS.md`) — la campagne devient une ROUTINE reproductible par n'importe quel LLM, sans reconstruire le contexte.** Constat : les briques existaient (orchestrateur `scripts/chaos/orchestrator.py`, design `banc-test-chaos.md`, entrées vault) mais AUCUN doc unique « comment lancer ». Le runbook consolide : (0) les 8 invariants, (1) prérequis + PIÈGES gravés (collecteur :9999 multi-jours SANS date → filtrer par OFFSET de ligne jamais par heure ; compteurs :9091 ne survivent pas au SIGKILL → mesurer par SEQ ; silence collecteur ≠ mort → croiser :9091 ; notifications background menteuses → marqueur EXIT + ps), (2) étage L `cargo run --bin tom-stress -p tom-stress -- invariants --seed 42` (in-process, CI, exécutable près de la flotte car TEST-* transparent), (3) étage F `python3 scripts/chaos/orchestrator.py --seed N --scenarios 1,2,3,7` (vraie flotte : inventaire+endpoints, #7 mesuré par seq deux-vagues-tailles-distinctes), (4) vérité terrain post-campagne (`taille_reseau` STABLE = anti-ravivage OK, jamais conclure sur un proxy — croiser paths_by_peer+collecteur), (5) durcir le banc, (6) checklist express. Le banc « grandit avec le code, jamais fini ». | La reproductibilité des tests ne dépend plus de la mémoire d'un LLM particulier ; onboarding test = lire 1 doc | `docs/plans/RUNBOOK-TESTS.md` (pointé depuis `banc-test-chaos.md`), orchestrateur `scripts/chaos/orchestrator.py` |
| 2026-07-18 nuit | **🌙 BOUCLE NUIT — 9 chantiers livrés builds 117→125, tous gate+FFI+CI verts, hot-path reviewés indépendamment.** Mode « chantier → face de test durcie → chantier » demandé par Malik (10-12h autonomes). **(1) Transparence TEST-* (117)** : on N'ISOLE PLUS les nœuds de test, on les MARQUE — `tom_protocol::TEST_NODE_PREFIX="TEST-"`+`is_test_node_username()`, tous les harnais tom-stress préfixent, tom-tui badge+jamais auto-connect+`select_ping_target` exclut+event collecteur `pair_test_trouve`, apps Swift badge 🧪. **(2) Orchestrateur étage F + compteur echoues honnête (118)** : `scripts/chaos/orchestrator.py` (scénarios #1/#2/#3/#7-#8 sur vraie flotte via :9300/:9091/collecteur, #7 mesuré par SEQ au collecteur — les compteurs :9091 ne survivent pas au SIGKILL — fenêtres par OFFSET de ligne, jamais par heure) ; Swift `messages_echoues` était un LITTÉRAL 0 → vrai compteur sur les 4 catch d'envoi. **(3) P0-1 binding PeerAnnounce (119)** : `handle_peer_announce` accordait Online sur simple timestamp → n'importe qui marquait une victime Online + forgeait username/app_build ; gate `signature_valid && node_id==from` (voie gossip déjà Known-only ADR-011). **(4) M1 re-dial présence-attestée+chemin-mort (120)** : ferme le finding T1 (ATV aveugle 90 s) ; `RedialGovernor` pur (throttle 60 s/pair, cap 3 FIFO, saturating_sub), déclenché PeerStale+mark_failed si `quorum.fresh_witnesses>=1`, effet `RedialStalePath` (lookup DHT détaché+reprobe), reset throttle SEULEMENT sur trafic DIRECT signé (via vide — finding review) ; event RedialTriggered RETIRÉ du FFI (décision #6, observabilité=trace). **(5+6) Anti-ravivage M1+wart (121) + M2+M3 (122)** : le tick reconnect 15 s embarquait `topology.peers()` EN ENTIER → redialait des centaines de fantômes (280-1286 mesurés) ; filtre d'âge (recent<5min OU Online/Stale, cap 16) + wart `last_seen=max(existant,clamp)` + `relay::TOPOLOGY_TTL_MS=24h` (élague non-Online >24h au save/load/tick 60 s, Online jamais évincé). **(7) sanitize username (123)** : voies gossip+directe acceptaient un username brut (bloat/`\n` casse JSON collecteur/injecte UI) → `discovery::sanitize_username` (32 octets, sans contrôle). **(8) P3 (124)** : `tom-chat --isolated` publiait ENCORE dans le rendez-vous DHT partagé → `enable_dht=!isolated`. **(9) Reset de cache (125)** : bouton Settings (2 niveaux réseau/usine, actif nœud arrêté, confirmation) + API `/reset` :9091 gated #if DEBUG + :9300 NAS ; **self-review a attrapé un vrai bug** (stop() détache le teardown FFI → effacer juste après = course SQLite → resetNode async attend `await node.stop()` avant d'effacer). **VALIDÉ RÉEL** : flotte redéployée 122 (Mac/iPhone/iPad/NAS, ATV retirée le soir — Malik regarde la télé), campagnes seed 42-45 **0 FAIL**, anti-ravivage confirmé (`taille_reseau` STABLE 155→155 vs 951→1286 le 17/07, 0 storm), **bug terrain guéri** (Mac émission morte = 280 fantômes state.db, purge A/B → guérison 15 s). **Résiduel honnête** : `taille_reseau` absolu élevé = pollution rendez-vous DHT par mes propres harnais de la nuit (<24h, transparents/badgés, P3 pour tom-chat load) ; M2/M3+P0-1+M1 sur le parc au prochain rebuild XCFramework (redéployés en 122 ; 123-125 = Swift/tom-tui). | 9 brèches colmatées + finding T1 fermé + anti-ravivage bout-en-bout ; discipline gate/review/versioning tenue sur 9 pushes | builds 117-125 (`bd525c5`→`5a4c16a`), mémoire `tom-night-loop-2026-07-18`, docs/plans (redial §3ter, anti-ravivage, reset-cache), orchestrateur `scripts/chaos/orchestrator.py` |
| 2026-07-18 | **🧪 BANC DE TEST CHAOS — conception (build 115) + étage L codé (build 116, scenario_invariants.rs) : fin des « campagnes chemin heureux », place à la VÉRIFICATION D'INVARIANTS DURS sous faute.** Constat Malik : nos campagnes validaient surtout envoi/réception/chrono — trop peu. Doc `docs/plans/banc-test-chaos.md` : 8 invariants durs (0 perte finale, ACK⟺livraison, reconvergence bornée, 0 zombie/gel/crash surprise, mémoire bornée, 0 double-livraison) + 21 scénarios gradués #1→#21 (dont #18 contact inconnu+offline). Un test PASSE ssi les invariants tiennent, pas si le RTT est bas. **Étage L livré** (`crates/tom-stress/src/scenario_invariants.rs`, commande `invariants --seed`) : chaos KILL/REVIVE/SKEW à **identités persistantes** (revive = même node_id via identity_path) + trafic chat numéroté tracké → **assertions RÉELLES vérifiées** : I1 `delivered=13/13 au BON destinataire, 0 perte` (payload encode expéditeur+destinataire, tâches réceptrices tokio, reconciliation par destinataire), I2 `13 confirmés, 0 fantôme` (received ⊆ sent), I3 `reconvergence RÉELLE 2,5 s < 15 s` (kill→reprise mesuré au bon instant), I5 `pas de gel pendant NI après le chaos`. ⚠️ **Une review indépendante a rattrapé 3 FAUX PASS de mon 1er jet** (I2 testait « ≥1 livré », I3 mesurait fin-scénario−revive = artefact 9,6 s, I5 ne testait qu'à la fin) — corrigés, d'où les mesures réelles ci-dessus. **I8 tranché N/A applicatif** (piège vérifié sur pièces) : `send_message` = UUID unique par envoi (`envelope.rs:46`), donc 3 envois du même contenu = 3 messages LÉGITIMES, pas un replay ; dédup router = `msg_id:from` (`router.rs:134`) protège le REJEU du même envelope ; vrai anti-replay = test router `nonce_replay_detected`. I4/I6/I7 (zombie/crash/mémoire) = étage F (flotte réelle). **INCIDENT + DÉCISION** : les nœuds de test ont été vus par la vraie flotte (fantômes `invariants-N` sur l'iPhone) — 4 canaux de découverte coupés (n0/mDNS/DNS-fallback/relay-urls) n'ont PAS suffi (canal résiduel du relais NAS non identifié ; impact réel limité, Mac resté sain 3 pairs, fantômes inertes ADR-011). **Malik tranche : on N'ISOLE PAS, on joue la TRANSPARENCE** — marquer explicitement les nœuds de test, les lister, avertir les vrais nœuds de leur existence/but (phase test ; la transparence suffit, on ne perd plus de temps sur l'herméticité parfaite). **Leçon process** : 2 rounds de sous-agent Codeur ont livré des assertions CREUSES (I1 « envois non-paniqués », I8 « documenté par architecture » — esquive du point dur ×2), repris à la main ([[verify-subagent-security-shortcuts]]). | Fin du test-chemin-heureux ; outil qui vérifie des garanties, pas des chronos ; virage transparence>isolation | `docs/plans/banc-test-chaos.md`, `crates/tom-stress/src/scenario_invariants.rs`, exécution seed 42 (I1-I5 verts), mémoire `banc-test-chaos-etage-L-wip` |
| 2026-07-18 | **🟥→🟢 CI TypeScript rouge (build 111) = FLAKE 1/256 dans 3 tests crypto, PAS un bug de production — cause trouvée dans le LOG CI réel, pas le screenshot.** Le screenshot utilisateur montrait « wrong secret key » (ligne 63), mais `gh run view --log-failed` accusait le test SUIVANT : `should reject decryption with tampered ciphertext` → `expected { secret: 'message' } to be null`. Cause racine : la falsification `` `${ciphertext.slice(0,-2)}ff` `` est un **no-op quand le dernier octet vaut déjà 0xff** (proba 1/256, l'octet change à chaque run via clé éphémère) → le ciphertext NON falsifié déchiffre correctement → le test attend null et reçoit le plaintext. La crypto (XChaCha20-Poly1305) est saine : elle déchiffre légitimement un message intact. Trois sites atteints du même anti-pattern (`encryption.test.ts:112` ciphertext, `validation.test.ts:59` ciphertext + `:71` nonce). Fix : flip GARANTI différent (`slice(-2)==='ff' ? '00' : 'ff'`) → `tampered ≠ original` par construction, quel que soit l'octet. Le 1/256 explique pourquoi 23 exécutions locales (8 runs complets + 15 isolés) n'ont RIEN reproduit (P(0 flake/23)≈91 %) et pourquoi la pre-push gate passait — un flake probabiliste ne se débusque pas par répétition courte, il se lit dans le code du test. 771/771 après fix. | Fin d'un rouge CI intermittent qui aurait resurgi 1 push sur ~256 ; leçon : lire le LOG CI réel (`--log-failed`), le screenshot pointait le mauvais test | `gh run view 29644337636 --log-failed`, `packages/core/src/{crypto/encryption.test.ts:112,validation/validation.test.ts:59/71}` |
| 2026-07-18 | **🔎 CONTRE-ENQUÊTE T2 (après-midi) : findings ② et ③ de la campagne INVALIDÉS — il n'y a eu NI veille Mac NI gel 45 s. La « cascade » de 09:40 = ARRÊT MANUEL séquentiel de 4 nœuds au bouton UI, suivi de redémarrages manuels.** Preuves croisées : campagne.log contient QUATRE « 👆 BOUTON Arrêter pressé (utilisateur) » — ATV 09:40:01, iPad 09:40:02, Mac 09:40:08, iPhone 09:40:49 — puis des « Starting node... up=0s » manuels (ATV 09:40:49, Mac 09:40:50, iPad 09:40:52, iPhone 09:41:09). Le Mac n'a JAMAIS dormi : `pmset -g log` = zéro entrée Sleep/Wake 09:39→09:45, chaîne `caffeinate -t 240` continue (mort 09:40:07 → suivant 09:40:08), collecteur vivant toute la fenêtre (lignes iPhone ininterrompues), PID app 43223 inchangé (même process, assertion re-créée 09:40:52, vérifiée live à 12:22). ZÉRO ligne watchdog/forceReset dans toute la fenêtre — le « cascade watchdogs » du rapport était une inférence, jamais un log. Le « gel 45 s » = le temps de passage de l'opérateur d'un appareil à l'autre entre Stop et Start. L'anti-veille macOS est CERTIFIÉE saine sur ses 5 chemins (`TomNodeService.swift` acquire :686 après `.running` ; releases :767 stop [celui qui a tiré — légitimement], :831 watchdog-start, :1107 foreground, :1254 net-transition) : l'invariant « assertion ⟺ running » demandé par le handoff existe déjà. Chantiers anti-veille et gel-45s FERMÉS sans code. Le finding ① (T1 : « présence attestée + chemin mort » sans re-dial, ATV aveugle 90 s) reste LE chantier — à re-vérifier sur pièces avant design (leçon du jour). Bonus involontaire : rolling stop/start de 4 nœuds réussi — survivants NAS+iPhone : livraisons continues (recu 5/5 #659-661 pendant la fenêtre), re-mesh <10 s par nœud au retour. Pièges de méthode épinglés : `head -N` qui tronque la vue (« iPad muet dès 09:39:54 » était un artefact de troncature) et narratif bâti sans chercher les actions humaines dans la fenêtre. | 2 chantiers fantômes évités (zéro code inutile) ; leçon forensique gravée : chercher l'action humaine (lignes BOUTON) et `pmset -g log` AVANT de conclure au spontané | `pmset -g log`, campagne.log:42159/42171/42181/42252, collecteur:765718, `pmset -g assertions` live, `TomNodeService.swift:686/767/831/1107/1254` |
| 2026-07-18 | **🏁 CAMPAGNE TOTALE FLOTTE 107 (matin, 09:18-09:44) : 1809 livraisons ✅ / 26 ❌ transitoires (98,6%), 0 perte finale, 0 crash process, 0 BOUCLE TENUE — et 3 findings débusqués en conditions réelles (⚠️ contre-enquête l'après-midi même : ② et ③ INVALIDÉS — voir ligne au-dessus ; seul ① tient).** Flotte 6 nœuds homogène 107 (fix #436 partout ; NAS md5-prouvé, ⚠️ IP DHCP changée .21→**.83**, service `tom-node`, status :8085, control :9300 `/send /sendall /metrics`). Scénarios : S1-S4 (379 msgs 1Ko→150Ko, 0 échec, 250 msgs/s encaissés), S5 SIGKILL ATV → reconvergence **8 s**, T1 iPhone→data cellulaire (continuité Mac/iPad/NAS, TROU ATV 90 s), T2 requalifié par la contre-enquête (arrêts manuels de 4 nœuds, ni veille ni watchdogs), autoguérison <10 s/nœud + rattrapage backup intégral au retour, T3 Laura LTE Free → re-upgrade DIRECT IPv6 cellulaire partout. **FINDINGS** : ① « présence attestée (quorum) + chemin mort » ne déclenche AUCUN re-dial relais — l'ATV a oscillé en-ligne↔vieilli 90 s sans tenter le relais (les autres avaient des chemins IPv6 chauds) ; ② la cascade de 09:40 = l'app Mac a RELÂCHÉ son assertion anti-veille en pleine activité (« Anti-sleep: system sleep re-enabled » 09:40:08, `TomNodeService.swift:559-570`, appelants 767/831/1107/1254) → machine endormie → perte du voisin ; ③ la perte de CE seul voisin a gelé ~45 s les runtimes iPad/ATV (watchdog les a ressuscités) → il reste un chemin bloquant sur perte de pair. Mineurs : RTT LTE 1758 ms toléré sans bascule relay, les 2 iPhones se labellisent « iPhone » (observabilité aveugle), compteurs persistants remis à zéro par forceReset. Rapport complet : scratchpad session `rapport_campagne_2026-07-18.md`. | Le protocole encaisse kill/arrêts manuels/cellulaire avec 0 perte ; chantiers ré-arbitrés par la contre-enquête : seul le re-dial presence-sans-chemin subsiste | Collecteur 43 978 lignes (horloge Mac unique), :9091 ×5 + NAS :9300, timeline recalée par `heure−uptime` |
| 2026-07-17 | **✅ FINDING PROPTEST MULTIPATH TRANCHÉ (soir même, sur pièces) : (A) VRAI BUG — l'enforcement `PROTOCOL_VIOLATION "peer failed to respond with PATH_ABANDON in time"` était LUI-MÊME le fautif ; port upstream n0-computer/quinn#436 EN ENTIER, gate débloquée.** Preuve (trace RUST_LOG=trace sur repro dédiée `regression_backup_passive_migration`) : le client abandonne path 1 et arme une deadline 3×PTO au 1ᵉʳ envoi du PATH_ABANDON (jamais prolongée par les retransmissions) ; la migration passive serveur casse la route porteuse → TOUTES les (re)transmissions perdues — **le serveur n'a reçu AUCUN PATH_ABANDON de toute la trace** (comptage frames reçues : 4 PATH_ACK_ECN, 2 PATH_RESPONSE, 1 CRYPTO) ; il sonde légitimement path 1 (encore en ouverture de son côté) ; une sonde arrive après la deadline → le client tue TOUTE la connexion. Punir le silence est indécidable : pair non conforme et réseau qui perd notre propre signal sont indistinguables. Upstream avait tranché à l'identique le 19/02 (`2903b55dd`, PR #436) : suppression de l'enforcement + compensation anti-DoS (crédit `MAX_PATH_ID` accordé à la RÉCIPROCITÉ du PATH_ABANDON, plus à l'abandon local) + enum `AbandonState` → bool `draining`. Port complet 4 fichiers (mod.rs, paths.rs, tests), doc de raisonnement `docs/plans/multipath-path-abandon-protocol-violation.md`, PAS (B) : élargir `allowed_error()` aurait masqué un défaut reconnu upstream — l'assertion faisait son travail. 2 régressions ajoutées (notre cas shrunk + scénario upstream adapté aux tuple-variants), seed `59a84bb8…` conservé dans `proptest-regressions/`. tom-quinn-proto **325/325 vert**, clippy workspace propre. | Gate débloquée le soir même ; doctrine « punir le silence = indécidable » gravée ; règle du port EN ENTIER respectée (vs faute #4314) | `docs/plans/multipath-path-abandon-protocol-violation.md`, upstream `2903b55dd`, `proptest.rs` (2 régressions), trace scratchpad |
| 2026-07-17 | **⚠️ FINDING OUVERT (honnête) : le proptest multipath `random_interaction_with_multipath_simple_routing` (tom-quinn-proto) échoue sur un NOUVEAU cas débusqué par le hasard — bug LATENT pré-existant, PAS une régression de la session.** Crate INTOUCHÉ depuis le build 71 (`84f164c`) ; le property-test a tiré à 20:38 un seed qui échoue et l'a persisté dans `proptest-regressions/` (rejeu déterministe désormais). La gate est passée toute la journée parce qu'elle tirait des seeds qui passaient — le bug dormait. Séquence minimale (shrink) : `OpenPath(Client,Available,1)×2 → Drive → ClosePath(Client,1,0) → PathSetStatus(Server,0,Backup) → PassiveMigration(Server,0)` → `assert allowed_error(poll_to_close(client))` échoue (`proptest.rs:118`). Interprétation ouverte : soit vrai bug de fermeture/migration de chemin multipath, soit `allowed_error()` ne liste pas ce cas de fin légitime. NE PAS masquer avec `#[ignore]` (cacherait un vrai bug). **BLOQUE la pre-push gate** → le commit doc du jalon reste LOCAL en attendant (le CODE, lui, est déjà poussé à `20f7d13`). À investiguer en session FRAÎCHE, pas à 20h40 en fin de marathon. Le fichier `proptest-regressions/tests/proptest.txt` est conservé pour rejouer le cas. | Bug latent multipath tracké ; à trancher vrai-bug vs assertion-incomplète | `crates/tom-quinn-proto/src/tests/proptest.rs:106/118`, `proptest-regressions/`, seed `59a84bb8…` |
| 2026-07-17 | **🏆🍾 JALON PRODUIT — 2 iPhones (Malik + Laura, 2 personnes distinctes) échangent des messages ToM : d'abord en RÉSEAU CELLULAIRE via relais, puis en WiFi LAN en DIRECT IPv6 pair-à-pair.** Premier usage RÉEL du protocole entre deux humains sur deux appareils indépendants, hors du parc de test. Preuve collecteur (20:31-20:32) : rafales `📤 en cours → iPhone · 80eb9196` / `✅ délivré → iPhone · b76a43d2` et réciproque, `path=DIRECT` des deux côtés. Le `paths_by_peer` de l'iPhone montre `b76a43d2 (Laura) → DIRECT [2a01:e0a:14f:5da0:...]` = **connexion IPv6 directe téléphone-à-téléphone, sans relais, sans serveur** — le Saint Graal du P2P mobile atteint. Flotte au moment du jalon : Mac/iPad/iPhone-Malik/NAS/iPhone-Laura en build 104, 4 pairs chacun, 0 échec ; ATV au repos (TV). Verdict utilisateur : « jamais vu un soft aussi abouti… champagne ». Laura = node `b76a43d2`, iPhone 12 Pro, LAN 192.168.0.49, UDID 5F7DA86D-D802-5C1B-9180-2950A7DC1306 (⚠️ install : désinstaller d'abord l'ancienne app signée K22558HU63 gratuite + `-allowProvisioningDeviceRegistration` pour enregistrer l'UDID). | Le protocole fait CE POUR QUOI il existe, entre vraies personnes, en mobilité | Collecteur 20:31-20:32, `paths_by_peer` iPhone :9091, builds 104, session du 17/07 soir |
| 2026-07-17 | **🏆 FAUTIF EXACT TROUVÉ ET TUÉ (builds 104, soirée close mieux que midi) : le VERROU DU POOL tenu pendant les dials + connected_peers inline dans la boucle.** Le profileur `timed()` (posé 19:08, canari NAS) a nommé le coupable en 90 s : « BOUCLE TENUE 5280→8724→48496→**74312 ms** par: cmd:connected_peers ». Chaîne causale complète et prouvée : ① `get_or_connect` exécutait `endpoint.connect()` (timeout QUIC 10-20 s vers un pair mort) **en tenant le mutex de la map de connexions** → chaque dial fantôme verrouillait TOUT le pool ; ② `connected_peers` (poll de statut ~1 Hz) attendait ce verrou — rafales libérées à la même milliseconde (44751/31667/18386/5455 ms), signature du verrou-otage ; ③ la boucle runtime l'awaitait INLINE → gel total → plus d'ACK/probes → latences d'attestation 10-20 s (les multiples de 10 s enfin expliqués) → vieillissement Stale synchronisé → l'oscillation de toute la soirée. Les fantômes n'étaient que le CARBURANT ; le verrou était le MOTEUR. FIX 2 étages : `GetConnectedPeers` répond en tâche spawnée (TomNodeSender clonable, la boucle ne touche plus la requête) + **dial hors verrou** (check → drop → dial → re-lock bref + double-check dial-concurrent). CERTIFICATION 19:30-19:45 : 5 relevés 3 pairs/0 échec, ticks Mac 12/12 et ATV 12/12 parfaits (iPad 35/36, l'unique 120 s ≤ 1 ligne UDP perdue), **0 « BOUCLE TENUE »**, et LE test au-delà de midi : la visite de l'iPhone encore empoisonné AJOUTE un 4ᵉ pair proprement — il fauchait la flotte entière 2 h avant. Robustesse-sous-pair-pollué acquise. Reste : purge iPhone au retour ; chantier anti-ravivage (design d'abord) devient du confort, plus une urgence. | LE bug transport de fond de la journée (NAS ×2, iPhone 3G, oscillations) éliminé par construction ; profileur timed() légué en permanence | Journal NAS (BOUCLE TENUE), `connection.rs::get_or_connect`, certification 19:30-19:45, commits `a6146da`+`e0c8dd2` |
| 2026-07-17 | **✅ SOIRÉE RÉSOLUE — l'oscillation flotte (pairs 4→0→4) = TOPOLOGIE PERSISTÉE EMPOISONNÉE, prouvé par canari A/B et guéri par purge (niveau midi certifié 18:54).** Après 3 pas de bisect infructueux (revert #4314 build 102, drain sans verrou, ack async build 103 — l'oscillation survivait à tout), le canari a tranché : `state.db` du Mac contenait **1286 pairs** dont des dizaines de fantômes (nœuds de test de la journée — 25 cycles de harnais DHT actif, harnais réception, wedge tests) au `last_seen` ravivé EN BOUCLE par le gossip inter-pairs → tempêtes de dial vers des morts à chaque passe → **boucles runtime gelées par vagues** (métrique décisive créée ce soir : intervalles des ticks « 60 s » mesurés à 84-218 s) → latences d'attestation 10-20 s → vieillissement Stale synchronisé → oscillation. La borne-16 du bootstrap (fix « 951 fantômes ») ne protège pas du RAVIVAGE continu. **Preuve** : purge du seul Mac (18:21) → ticks `[60×7]` parfaits + 3 pairs tenus, pendant que les témoins pollués dérivaient (74-121 s, 0 pair). Purge iPad/ATV (⚠️ piège : le 1er kill devicectl par bundle-id a raté SILENCIEUSEMENT — vérifier le process par PID, `up=` du collecteur en juge) → **recette finale 15 min : 3\|3\|3\|3 pairs, ticks [60×12] zéro dérive, 0 échec — NICKEL niveau midi**. Méthode purge iOS/tvOS : app éteinte (kill par PID vérifié) + `devicectl device copy to` d'un fichier VIDE sur state.db/-wal/-shm, identité conservée. Chantiers durables ouverts : harnais `enable_dht:false` ; anti-ravivage protocolaire (ne rafraîchir `last_seen` que sur trafic direct prouvé + cap LRU du store, DESIGN D'ABORD). Bonus soirée : tracker de connexions persistant (transitions + âge à la perte, croisement :9091 4 nœuds) — le « 0 pair » local peut mentir pendant que les voisins voient les connexions. | Flotte certifiée stable niveau midi ; 2 métriques de vérité léguées (dérive ticks, tracker croisé) | Canari 18:21-18:30, sqlite3 state.db (1286 pairs), recette 18:38-18:54 « NICKEL », mémoire `topology-poisoning-ghost-peers-2026-07-17` |
| 2026-07-17 | **✅ PAIR-POISON identifié (boîte noire TomTrace, 1ᵉʳ jour de service) : « l'ATV a un trou » = l'iPhone pré-hotfix qui empoisonnait ses voisins.** Symptôme : ATV s'effondre en boucle (3 pairs → 0) pendant que Mac/iPad tiennent. Boîte noire : tous les starts ATV sains (<150 ms, rc=0) → l'armure hors de cause en 30 s de lecture. Collecteur : ATV saine → dial de l'iPhone 16:58:52 → effondrement en 9 s. Cause : l'iPhone (mode avion pendant le déploiement) tournait encore sur le build sourd-UDP — il émettait (dials, retries) sans rien entendre → tempête de handshakes semi-ouverts ; l'appareil le plus faible (ATV) casse le premier, les autres encaissent. Guérison : hotfix sur l'iPhone → flotte 5/5 en ~1 min (4 pairs partout), tenue 3 min vérifiée. LEÇONS : flotte en versions mixtes = dangereuse pendant un fix transport (déployer PARTOUT avant de juger un appareil) ; le pair le plus faible est le sismographe, pas le coupable. | Diagnostic en 10 min grâce à TomTrace + collecteur — épisode clos | Boîte noire ATV, collecteur 16:58:50-16:59:01, :9091 ×4, mémoire `tom-ios-stall-transport-wedge-2026-07-17` |
| 2026-07-17 | **⚠️→✅ RÉGRESSION build 100 attrapée en 45 min : surdité UDP silencieuse (port #4314 à moitié) — hotfix `7c28353`.** Symptôme terrain (utilisateur) : « personne ne se connecte sans l'iPhone » — en réalité TOUTE la flotte à 0 pair connecté (Mac 0, iPad 0, ATV 0) pendant que les messages continuaient via relais (recus qui montent, « Backup livré »). Mécanisme : la 1ʳᵉ moitié du port iroh#4314 tolérait les erreurs recv, mais l'agrégateur `IpTransports::poll_recv` gardait son `?` → un socket v4 en erreur (flap mode avion) court-circuitait le poll des sockets v6 SAINS → aucun waker enregistré → plus jamais de recv UDP, sans erreur ni log. Avant : l'erreur tuait l'endpoint = brutal mais auto-guérissant (restart). Fix = 2ᵉ moitié upstream : itération PAR SOCKET (`iter_mut` v4+v6), chaque socket pollé/toléré individuellement. **Preuve** : hotfix déployé sur iPad/ATV/Mac en excluant volontairement l'iPhone → reconvergence complète en ~1 min (3 pairs chacun + NAS) — mythe « iPhone clé de voûte » éliminé. LEÇONS : porter un patch de résilience EN ENTIER ; valider sous flap d'interface, pas seulement sous orage de redémarrage ; le « silence du collecteur » et le « 0 pairs » se diagnostiquent en croisant :9091 + sample du process. Bonus : identités fantômes « timing »/« 101e444c » dans le rendez-vous = mes harnais de test (DHT actif) — à passer `enable_dht:false`. | Régression sérieuse fermée le soir même — et le vrai test d'endurance du flap est maintenant couvert | Screenshots iPad 16:29, :9091 ×3 à 0 pair, sample Mac, diff upstream #4314 (partie ip.rs), reconvergence mesurée sans iPhone |
| 2026-07-17 | **✅ CHANTIER WEDGE TRANSPORT COMPLET (build 100) — l'enquête gel iPad/ATV a livré la cause structurelle ET la cure.** ENQUÊTE (archive `log collect` USB iPad, pid 4115, RunningBoard vide = zéro suspension) : gel « start après arrêt propre » = **stall transport 5 min 21 s** qui gèle TOUT le runtime tokio FFI → le timeout tokio -2 n'a jamais tiré (même domaine de panne que ce qu'il borne) → watchdogs Swift 14:00:19/14:02:29 impuissants (forceReset FIFO derrière l'appel gelé) → libération SPONTANÉE 14:05:09 (enveloppe 5-15 min = famille NAS) → **course des 2 recoveries** → « Node is already running » → `.error` TERMINAL. CAUSE STRUCTURELLE trouvée en lisant tom-quinn : `ConnectionRef::clone` → `from_arc` → **prend le mutex d'état pour bumper ref_count** → tout clone/drop de ref sous verrou tenu s'auto-deadlockait (mutex non réentrant) — LA signature gdb NAS « futex tenu sans détenteur visible ». CURE : ① ref_count → `AtomicUsize` hors mutex (clone ne verrouille plus JAMAIS, drop seulement à la dernière ref) ; ② warn lock_tracking émis APRÈS libération (log sous verrou = contention) ; ③ port upstream **iroh#4314** (une erreur recv isolée — interface qui flappe avion/handoff — ne tue plus l'endpoint : fatal seulement après 8 rounds tous-transports-en-échec) ; ④ inbox RemoteStateActor 64→256 (#4325 toujours ouvert upstream) ; ⑤ FFI : budget start tenu par le THREAD APPELANT (`recv_timeout` OS + gate atomique anti-nœud-tardif) + ~15 entrées bornées 2 s + drains **transactionnels** (un timeout ne jette jamais un batch — trou attrapé en relisant le sous-agent) ; ⑥ Swift : recovery idempotente (`recoveryInFlight`), `alreadyRunning` → retry backoff, boîte noire TomTrace sur disque. Tests : wedge simulé (workers parkés) → -2 dans le budget ; drain conservé ; 25 cycles stop→create→start sans gel ; workspace + stabilité 2 min verts. BONUS enquête : **le silence du collecteur MENT** (pertes broadcast UDP prouvées — iPad « muet » 8 min mais vivant, ATV muette mais compteurs :9091 montants) → toujours croiser `:9091`. | Fin de la famille de stalls 5-15 min (NAS × 2, iPhone 3G, iPad, ATV) — par construction, plus par contournement | os.log iPad (`log collect` USB), gdb NAS 13/07, `tom-quinn/src/connection.rs::from_arc`, PR upstream n0-computer/iroh#4314, tests `start_returns_minus2_even_when_runtime_is_wedged` + `bounded_drain_keeps_batch_when_runtime_wedged`, mémoire `tom-ios-stall-transport-wedge-2026-07-17` |
| 2026-07-17 | **✅ CAUSE RACINE du « démarrage figé » TROUVÉE + FIXÉE (chantier start borné/anti-zombie, build 99)** : `mainline::Dht::client()` (v6.1.1) **bloque son appelant** — `Dht::new` spawn le thread actor puis attend `rx.recv()` du `Check`, et l'actor commence par `Rpc::new` → `to_socket_address(DEFAULT_BOOTSTRAP_NODES)` → `to_socket_addrs()` = **getaddrinfo SYNCHRONE de 4 hostnames** (router.bittorrent.com, dht.transmissionbt.com, dht.libtorrent.org, relay.pkarr.org). En 3G dégradée le resolver traîne (~30 s/nom, cumulable) → `DhtDiscovery::new` → `RuntimeState::new` → `ProtocolRuntime::spawn` → `block_on` FFI gelé → actor Swift empoisonné (le bug terrain du matin). **Réseau COUPÉ ne reproduit PAS** (getaddrinfo échoue instantanément « no connections available » — vérifié harnais Wi-Fi off : bind 30 ms) : c'est le réseau DÉGRADÉ qui tue, pas l'absent. FIX 3 étages : ① FFI `tom_node_start` borné (bind sur tâche tokio + timeout `start_timeout_secs` défaut 20 s → rc **-2** + last_error, abort + reaper `node.shutdown()` si course — rien ne survit à un start expiré) ; ② `tom_dht::SharedDht` : init mainline sur **thread détaché**, `get()` non-bloquant dans la boucle, `wait_ready()` 10 s dans les tâches détachées (1ʳᵉ ronde rendezvous rattrapée, pas perdue) ; ③ Swift : `TomError.startTimeout` (rc -2) → forceReset + backoff 5→60 s + « Réseau indisponible — nouvelle tentative dans Xs » (fini le spinner menteur). Mesuré post-fix : start total **96 ms** (bind 91 + spawn 5), publish DHT détaché OK à +3,4 s. BONUS découverte : les tests du crate FFI **ne compilaient plus à HEAD** (4 inits périmés — check-ffi.sh ne lançait jamais `cargo test`) → réparés + étape 5/6 tests ajoutée au script. | Fin du start figé/zombie ACK — contrat FFI borné par construction | `mainline-6.1.1/src/{dht.rs:132,rpc.rs:1108}`, `crates/tom-dht/src/lib.rs::SharedDht`, `crates/tom-protocol-ffi/src/lib.rs::tom_node_start`, test `start_expires_on_stalled_network_then_recovers`, harnais réseau coupé/normal |
| 2026-07-17 | **🔴 BUG TERRAIN MAJEUR (reproduit par l'utilisateur, stress 3G/avion/wifi-toggle) : start Rust non borné → actor Swift empoisonné → nœud ZOMBIE qui ACK → messages perdus.** Mécanique complète caractérisée : (1) `tom_node_start` (FFI) se fige indéfiniment sur réseau faible (« démarrage figé depuis 22 s / 33 s / **601 s** », Live Log) ; (2) l'appel C gelé bloque l'executor de l'actor `TomNodeWrapper` → les `forceReset`/`stop` du watchdog s'empilent derrière → **boucle inerte**, UI « Nœud inactif » ; (3) l'instance PRÉCÉDENTE (start propre 11:51:35) jamais libérée = **zombie vivant** : mesuré fenêtre 11:56→12:08 pendant que l'UI disait inactif — **8 « MSG from iPhone », 8 « délivré → iPhone » (ACKs du zombie !), 10 chemins DIRECT** (ce que l'Apple TV affichait — personne ne « mentait », deux vérités simultanées), 26 échecs entremêlés, dégradation RELAY 12:07:28, restart spontané propre 12:09 (retour réseau). **GRAVITÉ : ACK zombie ⇒ pas de backup ADR-009 ⇒ messages perdus pour l'app — viole l'esprit décision #1** (ACK = livré à l'APP). Plan de fix cadré (prompt de reprise livré, nouvelle session dédiée) : ① borner `tom_node_start` (timeout ~20 s, chemin d'erreur SANS fuite d'endpoint sinon on recrée le zombie) ; ② instrumenter l'await réseau non borné dans `TomNode::bind` ; ③ vérifier watchdog→forceReset effectif post-fix ; ④ UI « réseau indisponible — nouvelle tentative » + backoff. | Chantier PRIORITAIRE ouvert — perte de messages réelle en mobilité | Captures Live Log utilisateur 11:15-11:25, collecteur 11:56-12:08, mémoire `ios-background-lifecycle-48` |
| 2026-07-17 | **⚠️ Stall NAS — RÉCIDIVE #2 (matin ~06:35, build 95) : mort-vivant PARTIEL cette fois.** Le nœud émettait encore (keepalives QUIC vivants, RTT 5 ms vu du Mac) mais ne traitait plus les enveloppes entrantes → n'ACKait plus → TOUTE la flotte en Échec vers Freebox (Mac 425, iPad 331, ATV 271, iPhone 187 lignes échec/retry) ; `:8085` et `:9300/peers` muets, `/send` répondait encore. Signature journal : `mdns subscriber is blocked, dropping item` en boucle + RemoteStateActor moulinant vers des nœuds de test MORTS (e8a4a7e0/342e6743 — 5491 messages_echoues accumulés). Threads : main+worker en `futex_wait`, I/O `ep_poll` vivante → cohérent avec la **saturation d'inbox actor (iroh #4325)** plus qu'un gel total (même famille que 2026-07-13). Preuves archivées NAS `/root/debug-freeze-2026-07-17/` (threads-wchan + gdb bt). L'instance de la NUIT avait fini **OOM-killed** (dmesg, VM 920 Mo). `systemctl restart` → la flotte a re-livré immédiatement (104-156 « délivré »/appareil en 2 min). Fix de fond toujours = chantier transport connu (inbox 16 + mutex open_path_ensure). | Récidive documentée avec preuves — le chantier transport upstream reste dû | `journalctl` NAS, `/root/debug-freeze-2026-07-17/`, collecteur 07:26-07:40, mémoire `tom-nas-stall-root-cause-2026-07-13` |
| 2026-07-17 | **✅ CYCLE DE VIE BACKGROUND iOS LIVRÉ (#48, build 98) — fin du zombie/flapping et du churn au retour foreground.** Constat utilisateur : app absente de Réglages > Actualisation en arrière-plan + « de plus en plus mauvais en revenant du background ». Diagnostic : AUCUN mode background déclaré (gel en plein vol → présence zombie → ~41 % d'échec des envois vers l'iPhone) + forceReset+start COMPLETS à chaque retour, même 5 s (flag posé dès `.background`). Zéro crash TomNode sur l'appareil (213 .ips tirés via `idevicecrashreport`, aucun TomNode ; « Données et analyses » vide = partage désactivé + une éviction d'app suspendue ne loggue RIEN). LIVRÉ : grâce 18 s `beginBackgroundTask` (retour court = aucun restart, validé « retour dans la grâce (9 s) ») ; arrêt PROPRE fin de grâce + assertion tenue 8 s pour couvrir le teardown Rust détaché (sans ça : suspension en plein teardown → watchdog, observé puis corrigé) ; `UIBackgroundModes [fetch]` + BGTaskScheduler (app désormais VISIBLE dans Réglages, réveils ~30 s de relève backup, replanifiés à chaque background) ; MetricKit → Live Log + JSON data_dir/metrickit. Revue oracle 4 agents : 2 vrais trous corrigés (flags effacés par `.inactive→.background` sans `.active` ; stop() différé d'un réveil BG éteignant un nœud rendu à l'écran) + hold cross-cycle + collisions noms MetricKit ; rejetés sur pièces : « PII » dans l'id BGTask (= préfixe bundle public existant) et « retirer tvOS » (BGAppRefreshTask existe tvOS 13+). VALIDATION TERRAIN : sortie réelle avec l'iPhone → « BG: arrêt propre — fin de grâce (18 s) » puis **0 échec flotte→iPhone** (vs 187-425/appareil le matin). Conforme décision produit « pas de daemon » (réveils courts opportunistes). Piège : le broadcaster UDP meurt avec le nœud → les logs post-quiesce sont on-device seulement. | Flotte stable avec appareils mobiles qui vont/viennent + diagnostic embarqué | Commits `8a4eeb2`+`09757ba` (build 98), tests devicectl réels, collecteur, mémoire `ios-background-lifecycle-48` |
| 2026-07-17 | **✅ BUG RÉCEPTION FLOTTE RÉSOLU (build 96) — `messages_recus=0` depuis build 86 = régression Codable SWIFT, PAS le transport ni le FFI** : #40 (`447097b`, build 86) ajoutait à `TomMessage` les champs sortants (`to`/`isAuto`/`deliveryStatus`) + CodingKeys SANS `init(from:)` custom → le décodage synthétisé exige toute clé non-Optional (`isAuto` — les défauts de propriété ne servent jamais au décodage) absente du JSON FFI → `keyNotFound` → `catch { return [] }` silencieux dans `receiveMessages()` → chaque batch reçu jeté, compteur à 0, flotte entière, ~9h. Les « Délivré » côté expéditeurs étaient VRAIS (le Rust de l'app recevait ET ACKait — décision #1 jamais violée ; seule la couche d'affichage Swift perdait tout). Preuves : os.log `Failed to decode messages: <private>` en boucle (`/usr/bin/log show --predicate 'subsystem == "org.tom-protocol.tom-node"'`) ; dernier « MSG from » du collecteur 16/07 21:40 = 7 min après le déploiement 86 ; traces executor `2 effects [DeliverMessage + ACK→NAS]` pendant que Swift affichait 0. Diagnostic par élimination : CLI iso-config-app reçoit → harnais FFI natif (`crates/tom-protocol-ffi/examples/reception_harness.rs`, API C comme Swift) reçoit → harnais avec l'IDENTITÉ de l'app reçoit (backlog backup ADR-009 à +5 msg/s) → seul le PROCESS app échouait → os.log → décodage. La chasse transport de la nuit (#46/46b/46c) poursuivait cette régression Swift (fixes conservés : corrects et durcissants). FIX : `init(from:)` explicite (champs wire requis, champs sortants `decodeIfPresent`+défauts) + test de contrat `TomProtocolKitTests` décodant le JSON FFI EXACT (échouait `keyNotFound isAuto` avant fix, 2/2 après) + les 8 `log.error` de décodage du wrapper en `privacy: .public`. Validé RÉEL : Mac recus 0→103, iPad 133, AppleTV OK (envois déterministes NAS `:9300`). **iPhone encore en 94 (dormait) — à redéployer.** Relecteur indépendant : RATIFIÉ, aucun autre Codable du package avec le même bug latent (vérifié type par type vs `types.rs`). Leçon de classe : tout modèle décodé depuis le FFI = `init(from:)` explicite + test du JSON wire + jamais de `catch → []` muet. | Réception rétablie sur toute la flotte + classe de bug verrouillée par test de contrat | Build 96, os.log unifié, collecteur, `TomModels.swift`, `TomMessageDecodingTests.swift`, mémoire `swift-codable-ffi-decode-contract` |
| 2026-07-16 | **✅ RELEASE 2.1.0 (build 92) — session vitesse + UX + durcissement, builds 81→92, flotte homogène déployée & mesurée.** (1) **Reconnexion réseau <5s ATTEINTE** (nœud <1s, flotte simultanée <18s ; avant 45s-2min) via 4 causes racines distinctes trouvées en LISANT les logs : dial transport/0 PARESSEUX rendu proactif (`ensure_connected` détaché au join, symétrique gossip, `b14d0ae`) ; `reprobe_relays().await` pré-boucle figeait le démarrage ~2min → détaché (`007fe7e`) ; **951 identités mortes** réinjectées au bootstrap gossip noyaient les vrais pairs ~4min → borné 16 pairs vus <5min (`bae25e8`) ; `unregister_inbound` supprimait le registre d'une NOUVELLE connexion au re-dial → conditionné à `stable_id` (`4b44059`). (2) **UX Messages** : statut de livraison bout-en-bout (Pending→Sent→Relayed→Delivered→purgé), le tracker Rust le suivait mais rien ne traversait le FFI → nouvel event `MessageStatusChanged` + drain `status_changes`→FFI ; affichage « moi ≫ destinataire » + badges AUTO/MANUEL (`447097b`, dédup no-op `1c98996`). (3) **Sécurité red-team** (2 agents) : CRITIQUE déni-de-reconnexion par timestamp futur dans `handle_role_announce` (`last_seen=announce.timestamp` sans borne → évince les vrais pairs) → clampé `min(ts,now)` ; HAUT OOM registre `inbound` non borné → cap 512 + purge fermées (`936a331`). (4) **Fantômes réglés** : rendez-vous DHT sans filtre d'âge → nœud mort injecté ~2h → `rendezvous_entry_fresh` rejette >10min, tolère skew futur (`5d3f27b`). (5) **UI** : badge chemin jamais vide (relais/chemin en cours/connecté au lieu de blanc, `44289a3`) ; Live Log dédup + français ; overscan tvOS ; ordre pairs stable ; handoff Wi-Fi↔cell ne relance plus le nœud (le transport migre déjà, fin des rafales 5G, `fa019fd`). **Version alignée 2.1.0** (package.json + Swift), tag `v2.1.0`, bundle git backup vérifié. Flotte Mac/iPad/iPhone/AppleTV/NAS en 92, 4 pairs DIRECT chacun, 0 fantôme. | Jalon majeur : réseau rapide + observabilité honnête + durci | Commits `b14d0ae`→`5d3f27b`, tag `v2.1.0`, mesures collecteur/`:9091`/`journalctl` réels |
| 2026-07-12 | **✅ Chantier macOS déjà entièrement livré (4ᵉ doc-drift trouvé cette session)** : la spec `docs/superpowers/specs/2026-06-08-app-macos-tom-design.md` et le roadmap listaient 5 lots "prêt à démarrer" alors que tout existait déjà (`TomNode-macOS` cible Xcode + scheme, xcframework avec slice `macos-arm64_x86_64`, `TomNodeService.swift` avec `ProcessInfo.beginActivity` anti-veille macOS, entitlements réseau, cibles Makefile `macbuild`/`macrun`/`macdoctor`). Vérifié RÉELLEMENT (pas juste grep) : `make macdoctor` tout vert, `make macbuild` → `BUILD SUCCEEDED`, `.app` produit et LANCÉ en process natif sur le MacBook Pro (confirmé `ps aux`), fermé proprement. Reste à valider avec le NAS joignable (connexion P2P réelle, hors scope aujourd'hui). | Gain de temps majeur — pas de chantier à refaire | `apps/tom-node-tvos/{Makefile,TomNode-macOS.entitlements,TomNode-macOS/}`, build réel 2026-07-12 |
| 2026-07-12 | **✅ Red-team L1-003 post-livraison → 2 failles réelles trouvées+corrigées** : (a) pas de dégradation Online→Stale à l'effondrement du quorum (pair promu restait Online indéfiniment — Sybil peut construire un quorum temporaire, se taire, victime croit le pair vivant pour toujours) ; (b) `PresenceScope::Peers` d'un abonnement sans borne de taille (DoS asymétrique consommateur→relais). Fix (a) dans `tick_presence_cleanup` (`PEER_ONLINE_STALE_MS=60s`) — **piège trouvé en review** : le 1er jet dégradait vers `PeerStatus::Known`, ce qui aurait bloqué le pair définitivement (`HeartbeatTracker::check_all` ne remonte Online que depuis Stale/Offline, jamais Known — deux mécanismes de liveness coexistent avec des états séparés, `last_heartbeat` HashMap propre au tracker vs `PeerInfo.last_seen`). Corrigé vers `Stale` + event `ProtocolEvent::PeerStale` émis (cohérent avec le chemin heartbeat existant). Fix (b) : scope borné à `MAX_VIEW_ENTRIES=256`, rejet complet si dépassé. 625 tests verts, 2 reviews sécu indépendantes. | Cœur défensif L1-003 durci, round 2 | `crates/tom-protocol/src/{relay.rs,runtime/state.rs}`, `crates/tom-protocol/src/discovery/heartbeat.rs` (mécanisme préexistant compris en cours de review) |
| 2026-07-12 | **✅ Réaudit "Known Limitations" (CLAUDE.md) — 5 des 7 trous listés étaient déjà corrigés, doc jamais mise à jour** : #1 connexions zombies (`loop.rs:591-607` liveness_is_stale déjà câblé dans la décision d'isolement), #2 rendez-vous DHT squattable (`loop.rs:862-877` `rendezvous_entry_authentic` signature Ed25519 déjà vérifiée avant acceptation — reste juste le nombre de slots à 8, mineur), #3 FFI double-teardown (`tom-protocol-ffi/src/lib.rs:435-454` déjà fusionné en un seul `detached_teardown`), #6 adresses DHT non filtrées (`loop.rs:812-821` `direct_addr_is_dialable` déjà filtre, délibérément différent de la stricte relay_url pour garder le LAN), #7 nonce sans purge TTL (`router.rs:29-37` déjà TTL 24h + cap LRU, marqué R11.2 complet). CLAUDE.md corrigé avec preuves file:line. Plus AUCUN trou "Critique" ouvert. Leçon : ne jamais lancer un chantier depuis cette doc sans revérifier le code d'abord — elle a menti une fois. | Anti-hallucination / hygiène doc | `CLAUDE.md` §Known Limitations, réaudit 2026-07-12 |
| 2026-07-12 | **⚠️ Bypass sécu introduit par sous-agent Codeur, trouvé et corrigé avant merge** : le premier jet du spot-check crypto `verify_presence_entry_proof` (`state.rs`) avait `if entry.ack_proof.is_empty() { return true; }` — un raccourci "compat tests" qui annulait TOUTE la vérification en prod (un témoin malveillant envoie juste une preuve vide et bypasse les 7 checks). Trouvé en relisant le rapport du sous-agent avant tout commit, corrigé (bypass supprimé, `signed_view_from` reconstruit une vraie ACK Envelope signée pour les tests), test de régression `empty_ack_proof_never_promotes` ajouté. Leçon : toujours relire un raccourci "pour les tests" écrit par un sous-agent dans un gate de sécurité — ne jamais faire confiance au rapport de complétion seul. | Process — review obligatoire avant commit | `crates/tom-protocol/src/runtime/state.rs::verify_presence_entry_proof`, corrigé avant push |
| 2026-07-12 | **✅ L1-003 étapes 4-5 livrées** : cap par témoin anti-Sybil (`MAX_PEERS_PER_WITNESS=64`) + spot-check crypto réel du `proof_ref` (`ack_proof` = Envelope Ack signé brut, vérifié via `Envelope::verify_signature()` existant, pas de nouvelle logique de signature dupliquée). 619 tests tom-protocol verts, clippy 0 warning. Limite Sybil multi-identité documentée comme connue/acceptée (pas fermée ce sprint). | Cœur défensif L1-003 durci | `crates/tom-protocol/src/presence/{quorum,witness,relay_view}.rs`, `runtime/state.rs`, `docs/plans/L1-003-vue-signee-relais.md` §9 |
| 2026-07-12 | **✅ L1-003 étapes 1-3 livrées (§5 ADR-011, appareil faible s'abonne à la vue signée du relais)** : type wire `RelayPresenceView`/`PresenceEntry` (`0c78e70`) ; côté témoin `WitnessLog` + câblage depuis ACK relayés (`f5fa9dc`, `eee6a1f`) ; publication/abonnement/vue signée vérifiable (`54dcad4`) ; **quorum consommateur** `presence/quorum.rs::QuorumAggregator` — promotion Known→Online seulement si ≥N témoins DISTINCTS concordent (dedup témoin, TTL 30s, N dynamique 2-4) (`094ec96`). Ferme le kill-shot #3 (eclipse par relais unique) — un relais menteur seul ne peut plus rien promouvoir. Reste étapes 4-6 : durcissement (spot-check `proof_ref`, actuellement asserté pas vérifié), tests adversariaux réseau, validation flotte réelle (XCFramework + 5 appareils, pas fait). | Cœur défensif présence à l'échelle | Commits `0c78e70`→`094ec96`, `docs/plans/L1-003-vue-signee-relais.md` |
| 2026-07-11/12 | **✅ Red-team Fable ADR-011 PoP à 1M — 3 des 6 kill-shots fermés** : #1 présence gatée sur `signature_valid` (`ecee492`) ; #4 `Known` (adresse) séparé d'`Online` (travail soutenu prouvé) — fin du KNOWN 14h quasi-gratuit sur 1 relais (`aadc8bd`+`76bd63a`) ; #3 quorum témoins (voir entrée L1-003 ci-dessus). **Restent ouverts** : #2 (sweep de challenge actif O(N), pas encore dérivé du flux ACK), #5 (pas de cap agrégé global, seulement per-identité), #6 (métastabilité guérison de partition). | Anti-Sybil PoP durci mais pas soldé | `docs/plans/POP-PROOF-OF-PRESENCE.md`, commits build 33→35 |
| 2026-07-12 | **⚠️ Incident email d'auteur `hmail`** : 148 commits poussés sous `karaoui.malik@hmail.com` (typo, override env au moment du commit) → non attribués sur GitHub malgré `git config user.email` correct. Historique réécrit (filter-branch hmail→gmail + force-push) + **garde-fou mécanique ajouté au pre-push gate** (`42142b7`, check 0/5 : refuse tout push si auteur/committer ≠ gmail, comptes de service tolérés). | Hygiène attribution GitHub | Commits `42142b7` + réécriture historique 2026-07-12 |
| 2026-07-12 | **⚠️ Gap versioning observé** : `TomVersion.build` resté à **35** (`c20192f`) alors que 2 commits `feat` ont été poussés après (`54dcad4`, `094ec96`) sans bump — contredit la règle « bumper à chaque push ». À corriger/vérifier à la prochaine reprise. | Dette versioning | `sdk/swift/TomProtocolKit/Sources/TomProtocolKit/TomVersion.swift:12` + `git log` |
| 2026-07-05 | **⚠️ Stall récurrent du nœud de contrôle de test** : `tom-chat --bot` se fige après ~15 min (log gelé, process vivant) — probablement le serveur HTTP de contrôle (TCP brut) qui bloque le nœud. Harness de test uniquement, PAS le protocole. | Fiabilité harness | Observé sessions test 2026-07-05 |
| 2026-07-05 | **⚠️ Écart rôles à trancher** : rôles fondateurs BMAD = client/relay/observer (story 3-2-dynamic-role-assignment) ; code `PeerRole` = Peer/Relay seulement (observer abandonné au port Rust, client→Peer renommé). À décider : réintégrer observer ou documenter l'abandon. | Alignement fondations↔code | `_bmad-output/implementation-artifacts/3-2-dynamic-role-assignment.md` vs `crates/tom-protocol/src/relay.rs:19` |
| 2026-07-05 | **✅ Perf validée build 17/18** : LAN (iPad/Apple TV) ~6 Mo/s jusqu'à 64 Mo, 100% livré ; iPhone WiFi/relais ~5 Mo/s (montée relais→direct visible) ; 64 Mo livré sur les 3 appareils EN FOREGROUND. Seule vraie limite = suspension iOS en arrière-plan (contrainte OS, chantier R18 APNs). | Capacité confirmée ≥64 Mo à ~6 Mo/s LAN | `docs/test-campaign/speed_capacity*.log` |
| 2026-07-05 | **✅ Build 18 déployé sur TOUTE la flotte** (iPad, iPhone, Apple TV, macOS, NAS) avec les 4 fixes DoS + fixes CPU/watchdog. Wire format inchangé, interop OK. | Durcissement live de bout en bout | `TomVersion.build=18`, deploy 2026-07-05 |
| 2026-07-05 | **✅ DoS mémoire réassemblage (amplification 1-paquet) corrigé** : `vec![None; total_chunks]` allouait proportionnellement au total_chunks annoncé → 1,5 Go depuis 1 chunk. Fix : BTreeMap sans pré-alloc + `MAX_CHUNKS=100k` + budget global `MAX_TOTAL_REASSEMBLY=128Mo`. | Plus d'OOM depuis 1 paquet malveillant | Commit 347421b, `crates/tom-transport/src/protocol.rs` |
| 2026-07-05 | **✅ Review globale (Fable finders + vérif Opus/manuelle) → 4 bugs DoS classe "collection non bornée d'un pair non fiable" corrigés + testés** : (a) backup `replicated_to` cap `MAX_REPLICATED_TO` ; (b) group HubShadowSync `members` truncate `MAX_GROUP_MEMBERS=50` ; (c) réassemblage cap `MAX_CONCURRENT_PER_PEER=16` + TTL 30s zombies ; (d) DHT `relay_urls`/`direct_addrs` `.take(MAX_DHT_ADDRS=32)`. +6 tests régression. NB : la vérif Haiku était peu fiable (a classé gossip `pending()` comme critique = faux positif, idiome correct). | Réseau durci avant diffusion grand public | Commits 1656abb, 3d8cdfb + review workflow |
| 2026-07-05 | **✅ Chauffe device / spin CPU 100% root-causé + corrigé** : `tokio::select!` de `runtime/loop.rs` avait 3 branches à binding irréfutable (mDNS bootstrap_hint, relay peer_present, gossip) qui busy-spinnaient à 100% CPU quand le canal était FERMÉ (recv/next renvoie Ready(None) en boucle). Fix : remettre l'Option du receiver à None sur fermeture → select! bascule sur pending(). PLUS : `campaignEnabled=true` (outil test envoyant des gros fichiers toutes les 5s H24 même en background) → défaut false + throttle background. | iPhone/iPad ne chauffent plus, plus de kill EXC_RESOURCE | Commits 68a7022, dbf7d7c |
| 2026-07-05 | **✅ Kill watchdog iOS 0x8BADF00D root-causé + corrigé** : crash report `.ips` = "scene-update watchdog, main thread bloqué >10s". Cause : une vue SwiftUI `Text(message.text)` faisait le typesetting/césure (CFStringGetHyphenationLocationBeforeIndex → NSCoreTypesetter) d'un payload de plusieurs Mo sur le @MainActor. Fix : réception décode-lazy (aperçu compact stocké, jamais décoder un gros payload sur le main) + cap vue `Text(String(x.prefix(500)))`. | App iOS ne se fait plus tuer sous gros fichier | Crash `.ips` + commits 81b244a, dbf7d7c |
| 2026-07-01 | **🔴 5 criticals confirmés (audit 6-agents mobile) — PAS ENCORE corrigés** : (1) **failover hub mort au runtime** — `record_ping_failure` jamais appelé hors tests, `should_promote` toujours faux → hub tombe = groupes gelés (`manager.rs:846,880-887`) ; (2) **hub hijack** — `handle_hub_migration` pose `hub_relay_id=new_hub_id` sans authentifier l'émetteur (`manager.rs:449-465`) ; (3) **verrou #1 ACK entrant non gaté sur `signature_valid`** → ACK forgé marque "livré" (arm `Ack`, `state.rs`~872) ; (4) **verrou #2 purge SQLite hub** — `cleanup_hub_messages(TTL_MS)` au lieu de `now-TTL_MS` → jamais purgé (`state.rs:536`) ; (5) **dalek double-version** — tom-protocol `"2"` ≠ pin `=3.0.0-pre.1` (`Cargo.toml:23`). | Prochaine salve de fixes | `docs/audits/AUDIT-2026-06-26.md` + vérif code |
| 2026-07-01 | **✅ Souveraineté wire VÉRIFIÉE** : code 100% namespace `tom-*` — ALPN `tom-protocol/transport/0` (`tom-transport/src/lib.rs:116`) + `/tom-gossip/1` (`tom-gossip/src/net.rs:46`), SNI `.tom.invalid` (`tom-connect/src/tls/name.rs:19`), DNS `_tom`, `X-Tom-*`. ZÉRO identifiant iroh wire résiduel. **ToM PAS compatible iroh (souverain).** L'ancienne CLAUDE.md (« rester compatible iroh ») était FAUSSE — corrigée sur branche audit. | Correction doc majeure | grep code + commit `34f45bd` |
| 2026-07-01 | **Bruit git = bit exécutable** : 1302 fichiers "modifiés" = flip mode 644→755 (0 contenu), faisait échouer ultrareview. Réglé `git config core.fileMode false`. Reste 11 vrais non-commités (artefacts build + `config.rs`/`TomNodeApp.swift`). | Hygiène repo | `git diff --stat` |
| 2026-06-08 | **✅ Port UDP QUIC fixe désormais configurable (feature livrée)** : blocage (2) levé. Avant : `TomNodeConfig` n'exposait aucun bind → `builder.bind()` sans `bind_addr` → port OS éphémère. Ajouté : champ `TomNodeConfig.bind_addr: Option<SocketAddr>` + builder `.bind_addr()` + env `TOM_BIND_ADDR`, branché dans `TomNode::bind` via `Endpoint::bind_addr()`. Flags CLI tom-chat/tom-tui : `--bind-addr <ADDR>` et `--bind-port <PORT>` (raccourci `[::]:PORT` dual-stack, prioritaire = `--bind-addr`). Test `bind_addr_binds_fixed_port` vérifie `bound_sockets()`. Gate verte (clippy workspace 0 warning + workspace tests OK). **Reste** : redéployer NAS systemd avec `--key-path /root/<key>` + `--bind-port 43925`, puis ouverture IPv6 entrante Freebox (port désormais stable). | Feature transport — débloque IPv6 entrante | `crates/tom-transport/src/{config,node}.rs`, `crates/tom-tui/src/main.rs` |
| 2026-06-08 | **Test hardware réseau ToM** : NAS Freebox ajouté comme **nœud unifié** via service systemd `tom-node.service` (binaire `tom-chat --bot`, `role: Peer`, pas relais exclusif — ADR-006). NAS dual-IP `192.168.0.21`+`.83`, IP publique `82.67.95.8`, forward NAT UDP+TCP 3340 OK. Réseau 4 nœuds validé en LAN : iPhone `3c3d6d97…594e3e5`, iPad `71d47638…fa3c34b1`, Apple TV `c224961734…91927ea5`, NAS. node_ids mappés via logs (status-port `:8085` affiche préfixe 8 chars). | tvOS test session | `tom-gateway lan/status` (SDK Freebox), `curl :8085`, `journalctl -u tom-node` |
| 2026-06-08 | **Nœud NAS sans identité persistante** : node_id régénéré à chaque restart systemd (pas de `--identity-file`/`key_path`). À corriger pour ID stable. | Dette | `systemctl restart tom-node` → node_id change |
| 2026-06-08 | **Ouverture IPv6 entrante Freebox — bloquée, prérequis identifiés** : port UDP IPv6 du nœud NAS = `[::]:43925` (QUIC, pid tom-chat), IPv6 globale NAS `2a01:e0a:14f:5da0:248f:5dff:fea5:8ed1`. **2 blocages** : (1) l'API `/fw/incoming/` = ports des **services internes Freebox** (ftp/http/bittorrent…), PAS le filtrage IPv6 des machines LAN — endpoint correct **non confirmé**, ne pas POSTer à l'aveugle sur le firewall (§22) ; (2) le port `43925` est **éphémère** (nœud sans identité/port persistant → change au restart) → ouverture caduque. **Prérequis session fraîche** : (a) fixer port UDP + identité du nœud NAS (config systemd `tom-chat`/`--key-path`), (b) confirmer l'endpoint API IPv6 firewall Freebox OU le faire manuel dans Freebox OS → Mode avancé → Réseau → Pare-feu IPv6. | Reprise session | `ss -lupnH` (port), `curl /fw/incoming/` (endpoint observé) |
| 2026-06-08 | **🔥 DIRECT atteint en mobilité** : iPhone en déplacement (rue, cellulaire) ↔ NAS derrière Freebox en **connexion DIRECTE — RTT 98ms, 50 messages** (vs fallback relais 1856-3628ms). Preuve que le hole-punch QUIC aboutit quand le réseau cellulaire fournit une IPv6 publique ou un NAT « cone » (non-symétrique). DIRECT **variable** (« pendant un moment ») = mobilité (changement de cellule → réattribution IP/NAT → bascule direct↔relais). Conclusion : le CGNAT symétrique n'est pas systématique ; le DIRECT est atteignable, à stabiliser via IPv6 entrante Freebox. **Confirmé renforcé** : iPhone 5G à 1 barre (signal faible) tient **2 connexions DIRECTES simultanées** (NAS + Apple TV), **302ms, 300 messages** — robustesse multi-pair en mobilité dégradée. | Validation cœur ToM (NAT traversal) | Métriques app iPhone en mobilité |
| 2026-06-08 | **✅ Test 5G iPhone RÉUSSI** : après rebuild iPhone avec défaut `relayUrl=""` (découverte organique n0/Pkarr/DHT/IPv6), l'iPhone **en 5G hors-LAN derrière CGNAT opérateur** rejoint le NAS **derrière la Freebox** en **~1min30** (propagation Pkarr DHT). NAS `pairs_connectes:[iPad 71d47638, AppleTV c2249617, iPhone 3c3d6d97]`, échanges continus, **0 échec** (4266 env/4263 reçus). Preuve du modèle ToM décentralisé cross-réseau sans relais à IP fixe. Cause du blocage initial confirmée : relais `.70` privé forcé dans l'app empêchait le rendez-vous public. NB : `path_kind` (DIRECT vs RELAY) non exposé dans le status `:8085` — à instrumenter pour distinguer hole-punch direct du fallback relais iroh. | Jalon majeur tvOS/iOS | `curl :8085` (iPhone présent), `journalctl -u tom-node` |
| 2026-05-22 | **17 crates Rust** dans `crates/` — dont `tom-gateway` (CLI Freebox), `tom-relay-ffi`, `tom-integration-tests` absents de CLAUDE.md | Documentation à compléter | `ls crates/` |
| 2026-05-22 | **xcframework déjà buildé** : `apps/tom-node-tvos/build/TomProtocolFFI.xcframework/` + `libtom_protocol_ffi.a` présents | Phase 2 tvOS (FFI) complète en pratique | `ls apps/tom-node-tvos/build/` |
| 2026-05-22 | **App tvOS structurée** : `TomNode.xcodeproj` + `TomNode/` (Views, ViewModels, Models, Services) existe | Phase 1+2 tvOS bootstrap faites | `ls apps/tom-node-tvos/TomNode/` |
| 2026-05-22 | **tom-gateway** = CLI auto-config Freebox pour relay ToM (description: "CLI to auto-configure a Freebox router") | Fonctionnalité infra Freebox, non documentée dans CLAUDE.md | `crates/tom-gateway/Cargo.toml` |
| 2026-05-22 | **Version 0.2.0** pour tous les crates natifs (bumped 2026-04-15) | Versioning synchronisé via `chore: bump 0.1.0 → 0.2.0` | git log |
| 2026-05-22 | **Observabilité active** : format JSON unifié (appareil + node_id + uptime_s + msgs_sent), source_amorcage tracée | Système de métriques réseau temps réel entre nodes | commits 2026-04-15/16 |
| 2026-05-22 | **mDNS local discovery** activé par défaut depuis `b91b0fe` ("pompe d'amorçage distribuée") | Peers se trouvent sur LAN sans config — réduit dépendance relay pour bootstrap | git log |
| 2026-05-22 | **Relay reprobe** si topologie vide — `c88868a` reprobe relay quand aucun pair découvert | Résilience réseau : le nœud retente activement si isolé | git log |
| 2026-05-22 | **Wire invariants sacrés** : `_iroh` DNS prefix, `.iroh.invalid` TLS SNI, `X-Iroh-*` headers, `b"/iroh-qad/0"` ALPN | Ne jamais modifier — compat réseau iroh existant | CLAUDE.md §Wire Invariants |
| 2026-05-22 | **Effect pattern** : `RuntimeState` retourne `Vec<RoutingAction>`, le runtime loop exécute les effets | Pattern fonctionnel pur pour le state machine — facilite tests | CLAUDE.md §Effect Pattern |
| 2026-05-22 | **Self-send interception** : quand `hub == local_id`, ops group interceptées localement (pas de round-trip réseau) | Optimisation critique pour hub local — évite latence QUIC sur soi-même | CLAUDE.md §Self-Send |
| 2026-05-22 | **`iroh-quinn-udp` intentionnellement non forké** : `netwatch` expose ses types en API publique | Forker créerait mismatch de types — décision délibérée de ne pas forker | CLAUDE.md §Fork Status |
| 2026-05-22 | **Stress tests** : Campaign V5 250/250 Mac↔NAS (100%), 2748/2752 pings 4G highway (99.85%), hole punch 100% LAN/4G/cross-border | Validation production réelle — pas seulement CI | README.md |
| 2026-05-22 | **NAS Freebox** : Debian VM ARM64 Cortex-A72, `tom-relay --dev` port 3340, accessible `82.67.95.8:3340` (public) | Infrastructure relay propre opérationnelle | CLAUDE.md §Deployment |
| 2026-06-07 | **`cargo check --workspace` passe** sur macOS en ~65s | Le workspace Rust compile intégralement à date — bon signal de santé build | session terminal 2026-06-07 |
| 2026-06-07 | **Plan tvOS partiellement obsolète** : `docs/TOM-TVOS-NODE-PLAN.md` annonce encore création projet/Xcode/FFI/UI alors que `apps/tom-node-tvos/`, `crates/tom-protocol-ffi/` et plusieurs vues SwiftUI existent déjà | Risque de piloter le chantier avec une doc en retard sur le code | `docs/TOM-TVOS-NODE-PLAN.md` + `apps/tom-node-tvos/**` |
| 2026-06-07 | **`Packages/TomCoreKit/` absent** — les bindings Swift sont implémentés localement via `TomNodeWrapper.swift` + `TomNodeService.swift` | Écart entre architecture prévue et architecture réelle — clarification nécessaire avant de continuer | `file_search` + `apps/tom-node-tvos/TomNode/{Models,Services}` |
| 2026-06-07 | **Aucun test Swift/tvOS détecté** sous `apps/tom-node-tvos/` | La couche Apple TV a du code réel mais pas de filet de sécurité dédié | `file_search apps/tom-node-tvos/**/*Tests*.swift` |
| 2026-06-07 | **`runTests` par défaut part sur l'e2e Playwright** et tombe si `localhost:5173` est occupé | Friction de validation : le signal test global est pollué par la dépendance serveur local | `runTests` session 2026-06-07 |
| 2026-06-07 | **Working tree local très sale** : `.claude/`, `vault/`, artefacts `TomProtocolFFI.xcframework/`, lockfile et `project.pbxproj` non commités | Le signal "ce qui est livré" vs "ce qui est local" est brouillé | `git status --short` + `git diff --stat` |
| 2026-06-07 | **FFI tvOS entièrement câblé** : `TomNodeWrapper` (actor) mappe toute la surface C (`create/start/stop/free/send/group×2/add_peer/connected/discovered/status/receive/last_error/free_string`) ; `TomNodeService` orchestre polling 250ms, auto-echo, anti-sleep, export UDP, reprise foreground | Couche tvOS = vrai code productif, pas un squelette. Le wrapper local suffit. | `apps/tom-node-tvos/TomNode/{Models/TomNodeWrapper,Services/TomNodeService}.swift` |
| 2026-06-07 | **Vues SwiftUI réelles, pas cosmétiques** : StatusView 399L (double layout tvOS/iOS, listes cappées à 5 lignes pour télécommande), MessagesView 157L, SettingsView 170L, GroupsView 148L, LogView 91L, ContentView 39L (TabView) | Le chantier UI est avancé ; pas besoin de reconstruire | `wc -l apps/tom-node-tvos/TomNode/Views/*.swift` |
| 2026-06-07 | **`tom-protocol-ffi` est EXCLU du workspace Cargo** (`exclude` dans `Cargo.toml`) — crate staticlib standalone pour cross-compile iOS/tvOS | `cargo test -p tom-protocol-ffi` échoue depuis la racine ; utiliser `--manifest-path crates/tom-protocol-ffi/Cargo.toml`. Aucun crate du workspace n'en dépend (leaf). | `Cargo.toml` ligne 3 + `cargo test -p` échec |
| 2026-06-07 | **`make tvtest` existe mais AUCUNE cible de test n'est câblée dans `.xcodeproj`** (`grep XCTest\|TEST_HOST\|@testable` = 0) | Ajouter des XCTest Swift = créer une cible PBX + simulateur → pas la plus petite avance vérifiable ; filet de sécurité placé côté Rust à la place | `apps/tom-node-tvos/Makefile` + grep `project.pbxproj` |
| 2026-06-07 | **Point le plus fragile = contrats JSON stringly-typed FFI↔Swift sans test** ; pire cas `tom_node_status` construit à la main via `format!` (corruption possible sur caractère spécial) | Durci : passage à serde + tests de contrat (clés + échappement). Casse `PathKind::Display`="DIRECT" matche Swift `== "DIRECT"` par convention seulement | `crates/tom-protocol-ffi/src/lib.rs` + `crates/tom-transport/src/path.rs` |
| 2026-07-13 | **✅ Red-team réel validé sur QUIC live** : `tom-stress presence-attack` 6/6 PASS contre attaques forge (20 tentatives, 0 acceptées), replay (30, 0 acceptées), payloads corrompus, flood challenges (budget respecté), essaim Sybil 20 identités/300 msgs → cap GLOBAL tenu à 120 (vs 200 si cap par-identité seul, confirme kill-shot #5). `chaos-monkey` 6/6 PASS (4 kills/2 revives/10 clock-skews, réseau jamais mort). | Logique crypto L1-003/PoP validée adversarialement, pas juste en unit test | `docs/test-campaign/real-fleet-2026-07-13/`, `tom-stress presence-attack`/`chaos-monkey` |
| 2026-07-13 | **🐛 RÉAUDIT `tom-stress fleet-probe`/`campaign`** : (1) **Bug d'observabilité RÉEL, corrigé** — `fleet_probe.rs::print_report` écrivait le relevé JSON via `println!` brut au lieu du helper partagé `crate::events::emit()` (celui que `campaign.rs` utilise déjà) : le fichier `.jsonl` de fleet-probe restait TOUJOURS vide (0 octet) quel que soit le résultat réseau, alors que celui de campaign contient bien les vraies stats (vérifié : Burst 0/90, E2E 6/20, Failover 10/20, Endurance 0/300 — confirme les chiffres notés hier). Fixé (`fleet_probe.rs` route maintenant via `emit()`), `cargo build -p tom-stress` vert. (2) Conséquence : la conclusion d'hier "fleet-probe découvre 0 pair" n'est PAS re-vérifiable depuis le `.log` sauvegardé (`tracing_subscriber` n'y capture QUE les lignes émises par les macros `tracing::`, JAMAIS les `eprintln!`/`println!` du binaire lui-même — table de pairs et compteurs de fleet-probe absents du fichier par construction) ; seule l'observation live du terminal (faite hier) fait foi. (3) L'hypothèse "campaign échoue à cause de `enable_dht: true` par défaut (vs `false` dans fleet-probe)" proposée en 1ère analyse est **CONTREDITE** : le `.log` de fleet-probe (qui a pourtant `enable_dht:false`) montre EXACTEMENT le même symptôme `tom_gossip::net: failed to send: connection task send loop terminated` vers les mêmes 4 peer IDs mystères (`8cc99462b9`, `68ba2d92e7`, `fbba20fa47`, `b4175ac2b7`), dès ~25-45s après démarrage — donc pas spécifique au DHT. Root cause réelle de la dégradation post-Ping campaign toujours OUVERTE (ces peer IDs ne correspondent pas à la cible `--connect`, origine non identifiée — pas les 4 devices connus de la flotte a priori). Prochaine étape : relancer un run réel (JSONL fleet-probe maintenant fiable) avec `RUST_LOG=tom_transport=info,tom_gossip=debug` pour capturer l'activation mDNS + l'origine de ces 4 IDs. | Un vrai bug d'outillage trouvé+corrigé ; l'hypothèse DHT invalidée par preuve directe, pas juste supposée ; root cause réseau encore ouverte | `crates/tom-stress/src/fleet_probe.rs`, `crates/tom-stress/src/events.rs::emit`, logs `docs/test-campaign/real-fleet-2026-07-13/` |
| 2026-07-13 | **🐛 Stall NAS reconfirmé, plus rapide qu'attendu** : `tom-node.service` (systemd, NAS) devient silencieux (0 ligne journalctl, status-port TCP accepte mais ne répond jamais) après seulement ~5 min d'uptime cette fois (vs ~15min documenté avant) — process vivant, CPU 1,2% (pas de boucle folle), juste figé en interne. Restart systemd résout temporairement. Cause toujours non identifiée (probablement un await bloqué dans le runtime tokio partagé, cf. note antérieure "harness uniquement, PAS le protocole" — à re-vérifier, le délai raccourci suggère peut-être un lien avec la charge). | Stall reproductible, prioritaire pour prochain chantier | `journalctl -u tom-node`, `ss -tlnp` port 8085 accepte sans répondre |
| 2026-07-13 | **⚠️ Sprint instrumentation mDNS — ÉTAPE 1 de diagnostic, PAS une résolution du bug** : pipeline mDNS bloqué `Mac↔NAS zéro connexion malgré visibilité mutuelle LAN continue` reçoit une première couche d'observabilité ; **ne résout PAS la cause racine** (pourquoi `join_peers()` échoue systématiquement) mais permettra diagnostiquer au PROCHAIN repro en prod. Changements : (1) buffer subscriber mDNS augmenté `20→64` (`tom-connect/src/address_lookup/mdns.rs:432`) pour réduire saturation sur rafales d'events ; (2) log d'échec `join_peers()` remonté `debug!→warn!` (`tom-protocol/src/runtime/loop.rs`) + nouveau `warn!` si `gossip_sender==None` (impossible d'amorcer) ; (3) déduplication temporelle des hints mDNS dans `tom-transport/src/node.rs` : `HashMap<EndpointId, Instant>`, fenêtre 5s, purge FIFO tous les 100 hints (bug fuite mémoire non bornée découvert+corrigé PENDANT la review de ce sprint, pas dans le jet initial). Validation : tom-connect, tom-transport, tom-protocol builds ✅ ; `cargo test -p tom-transport` 42/42 ✅ ; `cargo test -p tom-protocol --lib` 632/632 ✅ ; clippy workspace 0 warning ✅ ; `cargo test --workspace` complet en validation finale (pas encore confirmé au moment de cette note). Prochaine étape : attendre la reproduction du bug Mac↔NAS pour voir les nouveaux `warn!` logs identifier la cause réelle d'échec de dial. | Instrumentation prélude / non-résolution | `crates/tom-connect/src/address_lookup/mdns.rs:432`, `crates/tom-transport/src/node.rs`, `crates/tom-protocol/src/runtime/loop.rs`, suite test workspace OK |
| 2026-07-13 | **🎯 ROOT CAUSE DU STALL NAS IDENTIFIÉE (forensique gdb sur process figé, PID 929) : deadlock mutex dans tom-quinn, gel total du runtime tokio.** Gel capté LIVE à 20:27:30 UTC pile pendant une rafale de 3 `voisin_connecte` simultanés (24 ms). Preuves : (1) les 2 seuls workers tokio (VM 2 cœurs) bloqués en permanence dans `std::sync::Mutex::lock_contended` — plus AUCUN thread en `epoll_wait` → I/O driver tokio orphelin → plus rien ne tourne (timers/logs/réseau morts, process vivant, CPU ~0) ; (2) chaîne d'appel symbolisée par correspondance binaire non-strippé + résolution nearest-symbol (deltas 0x200/0x210 concordants) : `RemoteStateActor::run → handle_path_event → insert_open_path/select_path → RemoteStateActor::open_path → tom_quinn::Connection::open_path_ensure → lock_contended` — IDENTIQUE sur les 2 workers, sur 2 connexions différentes (2 futex distincts) ; (3) les 2 futex valent `2` (tenu+contendu) alors qu'AUCUN des 4 threads du process ne peut être le détenteur (main = parking_lot condvar `block_on`, thread DHT = recvfrom) → **guard leaké / jamais relâché**, détenteur exact pas encore nommé (lock_tracking était OFF). Binaire diagnostic préparé : feature `lock_tracking` de tom-quinn activée (via tom-connect/Cargo.toml) + non-strippé → les logs nommeront les derniers détenteurs (`Locking the connection for X took…`) au prochain gel. Scène de crime archivée sur NAS `/root/debug-freeze-2026-07-13/` (core dump 53 Mo + binaire déployé + binaire symbolisé + symtab + stacks bruts). | Le « stall périodique NAS » n'est PAS un harness bug : deadlock transport réel, se déclenche sur rafales de connexions | gdb/futex/core sur NAS, `crates/tom-quinn/src/mutex.rs`, `crates/tom-connect/src/socket/remote_map/remote_state.rs` |
| 2026-07-13 | **🎯 BUG N°2 DISTINCT — le dial gossip NAS→Mac ne DÉMARRE jamais (pas un échec, une suspension infinie)** : en 40 min de logs, `dial failed: timed out` loggé pour 8 pairs WAN divers mais JAMAIS pour le Mac (`4acfab2bb2`) ; tcpdump 25 s sur le NAS : le Mac émet en continu vers le socket QUIC correct du NAS (`192.168.0.83:43550`, vérifié `ss -ulnp`), le NAS ne renvoie RIEN (0 paquet sortant vers le Mac, pas même un pong Disco), firewall VM vide (policy ACCEPT), pas de règle nftables. Mécanisme identifié = **issue upstream iroh #4325 (OUVERTE)** : l'inbox `mpsc::channel(16)` de chaque `RemoteStateActor` (`remote_state.rs:226`) sature pendant les connects concurrents ; `try_send` plein → datagrams (y compris Initials QUIC + retransmits !) **silencieusement dropés en signalant Ok** au QUIC layer → handshake affamé sans erreur ; les messages de contrôle (`ResolveRemote`, `AddConnection`) partagent la même queue 16 → le connect() du dialer gossip attend un oneshot qui ne vient jamais → **aucun timeout ne se déclenche, aucun paquet n'est émis**. Le commentaire du code l'avoue : « The actor is not processing items from the inbox while waiting on any async calls. So some discipline is needed » (`remote_state.rs:249`). Issues sœurs upstream à considérer au fix : #4390 (pending_open_paths VecDeque non bornée, multi-Go), #4293 (mapped_addrs jamais évincé), #4124 (open_path spam après MaxPathIdReached — notre `open_path` re-planifie toutes les 333 ms sur RemoteCidsExhausted). | Explique le Mac invisible même quand le NAS log normalement : l'actor du Mac est coincé AVANT le gel total ; 1er worker pinné tôt (dégradé silencieux), 2e worker pinné à la rafale (gel total) — cohérence parfaite avec la timeline | `https://github.com/n0-computer/iroh/issues/4325`, `crates/tom-connect/src/socket/remote_map/remote_state.rs:226,249`, tcpdump/ss NAS |
| 2026-07-13 | **✅ Sprint 1 « Transport Reliability » — LIVRÉ, non commité, en attente de validation utilisatrice + test flotte réelle** : 4 fixes mitigations bugs n°1+#2 (stall NAS + dégradation dial) : (a) `remote_state.rs:226` inbox RemoteStateActor 16→64 (upstream iroh #4325), ratifié 820 tests verts + clippy 0 warning ; (b) `transports.rs:~793` drop datagram sur inbox pleine : log debug!→warn! court endpoint (« RemoteStateActor inbox dropped message (buffer exhausted) ») + datagrams effectivement lâchés via QUIC (débat upstream, hors scope) ; (c) `config.rs` DEFAULT_RELAY_URLS **vidé** — les 3 relais publics relay-eu/us/asia.tom-protocol.org NXDOMAIN en prod (50 fails probe/20min sur NAS, reproduce du bug upstream gossip PR #149 « home relay injoignable + dials concurrents → mesh jamais formé ») — fallback : EXTRA_FALLBACK_RELAY (compile-time) + DNS TXT + découverte restée intacte, ADR-010 inchangé ; (d) 4 tests tom-transport adaptés (URLs locales, assertions). **Écart assumé** : compteur métrique inbox_dropped NON implémenté (pas d'accès propre à struct métriques depuis point de drop) — warn! seul, à re-évaluer. **Deliverable** : diff complet, non commité (git status), en attente du feu vert utilisatrice pour `git push`. Prochaine étape : test flotte réelle pairs_connectes Mac↔NAS (validation upstream #4325 mitigation). | Livraison et processus gate | `crates/tom-connect/src/socket/remote_map/remote_state.rs:226`, `crates/tom-connect/src/socket/transports.rs:~793`, `crates/tom-transport/src/config.rs`, tests tom-transport, 820/820 tests ✅, clippy 0 ✅ |
| 2026-07-14 | **🎯 RÉGRESSION DE FOND TROUVÉE ET CORRIGÉE — le signal de découverte tué par ADR-011 (« ghost-peer fix »)** : depuis ce durcissement, `PeerAnnounce`/`NeighborUp` faisaient uniquement `mark_known` (topologie, statut Known) SANS jamais émettre `PeerDiscovered` → les apps n'apprenaient jamais l'existence des pairs → l'auto-message ne se déclenchait jamais → personne n'envoyait le premier message → réseau mort à l'écran malgré transport/gossip fonctionnels (le réseau ne survivait que par injection manuelle d'un message, vérifié 3× ce soir). Fix : `surface_discovery()` (state.rs) — chaque pair réel (annonce reçue OU voisin gossip vivant) est surfacé UNE fois comme `PeerDiscovered` via un HashSet dédié `surfaced_peers` (dédup INDÉPENDANT de la topologie : les hints mDNS/AddPeer/DHT y insèrent les pairs silencieusement AVANT l'annonce, donc « nouveau en topologie » ne peut pas être le critère — 1er jet raté à cause de ça, corrigé). AddPeer et résultats DHT n'émettent PAS (anti-spam : pas d'auto-contact du bruit WAN). Sécurité ADR-011 intacte : signal pur, zéro crédit heartbeat/Online, testé (`handle_gossip_announce_registers_peer` mis à jour : émet 1× puis silencieux, statut reste Known). **Validation flotte réelle 01:46 : réseau AUTO-AMORCÉ sur les 5 nœuds sans aucune intervention** — Mac 9/6, iPad 3/5 (+ « 👤 Pair découvert » visible à l'écran), ATV 3/6, iPhone 6/0, trafic croisé iPad→ATV. | LA cause racine du « réseau mort depuis des semaines » ; chaîne complète enfin fermée : annonce → découverte → auto-message → connexion → sondes 30s | `crates/tom-protocol/src/runtime/state.rs` (surface_discovery, GossipInput::PeerAnnounce/NeighborUp), collecteur UDP campaign.log 01:46 |
| 2026-07-14 | **✅ Deadlock tom-quinn fixé et validé en réel** : `open_path_ensure` bras « path existant » construisait `OpenPath::new/ready` (→ `from_arc` → re-lock du même mutex `connection::State`) SOUS guard tenu → self-deadlock → gel total runtime (2 workers VM). Fix 4 lignes : calculer `recv` puis `drop(state)` AVANT la construction (pattern déjà présent dans le bras voisin — un oubli upstream). 3 gels reproduits ce soir AVANT fix (22:47, 23:07, ~23:20, forensique complète core+symbols+lock_tracking archivée NAS `/root/debug-freeze*`), 0 gel APRÈS déploiement. tom-quinn 23/23, clippy+workspace verts. | Fin du « stall périodique NAS » — à proposer upstream (n0-computer/quinn) | `crates/tom-quinn/src/connection.rs:424-434`, gdb backtraces symbolisées |
| 2026-07-14 | **⚠️ Test kill-app ADR-009 NON CONCLUANT (identité régénérée à la réinstallation)** : test de nuit — app iPad tuée (devicectl, 02:55), 5 messages 64 o envoyés vers son node_id `01f9bff9…` pendant qu'elle était morte (03:25, tous « ok » côté NAS). MAIS l'utilisatrice a réinstallé/relancé les apps le matin → **les node_id ont changé** (identité stockée dans Caches/, régénérée à la réinstall — iPad désormais `10ca19aa…`). Mes 5 messages visaient donc une identité morte : impossible d'affirmer que la rafale « AAAA… » de 09:09:29 est CETTE redélivrance (probablement du trafic de sonde/campaign d'un autre nœud). **Backup côté émetteur CONFIRMÉ** (« 📥 Backup stocké pour … » loggé côté Mac), mais la boucle complète reste à retester PROPREMENT : garder la même identité entre kill et relance (ne pas réinstaller entre les deux), ou cibler l'identité courante. À refaire. | Test à rejouer sans changement d'identité ; le backup-store lui est bien actif | campaign.log 03:25/09:09, reset identité Caches/ |
| 2026-07-14 | **⚠️ 2 crashs silencieux tom-node dans la nuit (avant 3h)** masqués par systemd Restart=always (NRestarts=2 constaté) — très probablement le flaky #17 (abort non-unwinding dans ConnectionRef::drop, PoisonError en destructeur). Mesures : RUST_BACKTRACE=full injecté dans l'unité (override backtrace.conf), journald rendu persistant (/var/log/journal) — le PROCHAIN crash livrera la backtrace complète. Le réseau s'auto-guérit (restart systemd + amorçage organique) donc sévérité moyenne, mais crash réel à root-causer. Depuis le reboot VM de ~3h : 6h19 d'uptime, 0 crash, 0 gel — le fix deadlock TIENT en durée. | Crash résiduel traqué, réseau résilient | systemctl NRestarts, /etc/systemd/system/tom-node.service.d/backtrace.conf |
| 2026-07-14 | **Apple TV « pause + effacement »** (constat utilisatrice ~09:09) : tvOS a suspendu l'app (perte de focus/économiseur) malgré l'anti-veille audio → au retour, restart complet du runtime → compteurs + activité (en mémoire) remis à zéro. Ce n'est PAS une perte réseau (les autres nœuds continuent, le backup couvre les messages ratés). Mitigation en chantier build 46 : persistance UserDefaults des compteurs cumulés + noms connus. | Limitation tvOS connue (#4), contournement UX en cours | campaign.log AppleTV uptime reset 09:09 |

---

## 🎯 RÉTROSPECTIVE 13-14 juillet 2026 — « le réseau mort depuis des semaines » : synthèse propre

**Contexte** : frustration utilisatrice « je lance Tom sur mes appareils et je vois quelques messages et ça s'arrête… régression depuis 15 jours ». Diagnostic → **cascade de 4 bugs indépendants**, chacun masqué par un indicateur vert mensonger. Tous corrigés, flotte remontée en puissance (5 nœuds : Freebox NAS, MacBook Pro, iPad, iPhone, Apple TV), build 41 → 48.

| # | Bug | Racine (file:line) | Masqué par | Fix | Statut |
|---|-----|--------------------|-----------|-----|--------|
| 1 | **Signal de découverte tué** (LE bug de fond) | `runtime/state.rs` PeerAnnounce/NeighborUp → seulement `mark_known`, jamais `PeerDiscovered` (durcissement ADR-011) | `phase:Converged` sur pairs CONNUS pas connectés ; réseau ne survivait que par injection manuelle | `surface_discovery()` re-émet le signal 1×/pair (sans crédit ADR-011), re-émet quand le nom arrive après un NeighborUp anonyme | ✅ commit `db2dafe`+`5fb59ac`, validé flotte réelle (auto-amorçage 5 nœuds sans intervention) |
| 2 | **Deadlock QUIC réentrant** (« stall périodique NAS ») | `tom-quinn/connection.rs:424` open_path_ensure construisait OpenPath::new sous guard tenu → from_arc re-lock même mutex | systemd Restart=always → présenté comme stall aléatoire | `drop(state)` avant OpenPath::new (pattern du bras voisin) | ✅ commit `1e4ec09`, 3 gels autopsiés (core+lock_tracking) puis 0 gel |
| 3 | **Inbox actor saturée** (handshakes jetés) | `tom-connect/remote_state.rs:226` mpsc(16) plein → datagrams QUIC dropés en signalant Ok (iroh #4325) | drop loggé en debug! + « dial failed » jamais émis pour le pair affamé | inbox 16→64, drop→warn!, relais fantômes NXDOMAIN retirés | ✅ commit `265f40b` |
| 4 | **Abort en destructeur** (flaky + crashs nuit) | `tom-quinn/mutex.rs:48,138` .lock().unwrap() sur mutex empoisonné → panic dans ConnectionRef::drop → abort | flaky ~1/N runs, crashs masqués par restart | `.unwrap_or_else(|e| e.into_inner())` — un destructeur ne doit jamais abort | ✅ commit `b8bc43e` (résiduel : panic racine à identifier) |

**Bonus livrés** : dashboard « zéro boîte noire » (events FFI câblés → écran + logs), sondes 30s/TTL 5min (ADR-009), noms d'appareils partout (Freebox/MacBook Pro/…), refonte écran activité-centrée, compteurs cumulatifs, persistance anti-reset tvOS.

**Reste ouvert** : #19 orage de rejoin gossip (NeighborDown→join_peers(8) en boucle, churn CPU + spam PeerStale), résiduel iOS suspension (#4 Known Limitations), panic racine du poison mutex.

**ENSEIGNEMENT CENTRAL** (voir mémoire `observability-must-reflect-ground-truth`) : la régression a survécu des semaines car **l'observabilité mentait** (vert sur des proxies dérivés, pas sur les connexions/livraisons réelles) et un **fix défensif a affamé un chemin vital en silence**. « Plus aucune boîte noire » était l'instinct d'ingénierie correct, pas un caprice UX. Prioriser la vérité terrain observable AVANT d'empiler des features ; tester tout garde-fou contre le flux sain qu'il pourrait couper.
| 2026-07-14 | **✅ Journée stabilité+UX (builds 48→51, tout poussé CI verte, micro-commits)** : (1) **#19 orage de rejoin RÉSOLU** — chaque GossipNeighborDown rejoignait TOUS les known_peers sans condition → dizaines/seconde (churn CPU + spam « Pair vieilli »). Throttle 3s (`loop.rs`) ; validé réel NAS build 50 : 0 rejoin, 0 stale, CPU 2,1% (avant : centaines). (2) **UI** : compteurs iPhone lisibles (fini le vertical), dédup activité, **noms d'appareils partout** incl. activité (« Freebox · 11d5bb11 » via `displayLine(resolvingNames:)` + fix `surface_discovery` re-émet quand le nom arrive après un NeighborUp anonyme). (3) **Refonte écran** activité-centrée (plus de scroll pour voir l'activité sur ATV/Mac). (4) **Dashboard Phase 2 sprint 3** : bandeau ATTAQUE rouge (SenderThrottled/MessageRejected/GroupSecurityViolation <60s) + horloge locale (honnête : pas de fausse dérive, le signal skew n'existe pas côté données). (5) **2 flaky intégration élucidés** : idle_then_reply (test trop strict sur LAN réel → filtre msg.from) + stability_2min abort (mutex.rs poison → unwrap_or_else into_inner). Push discipliné commit-par-commit avec attente CI verte à chaque étape (consigne utilisatrice). | Consolidation majeure : réseau stable+lisible, dette réduite | commits fe1c01b→c371837, builds 48-51 flotte, mémoire `observability-must-reflect-ground-truth` |
| 2026-07-14 | **✅ RED-TEAM re-validé post-changements du jour (presence-attack 6/6 PASS)** : après les fixes du 14/07 (surface_discovery, throttle rejoin, mutex poison), re-run adversarial `tom-stress presence-attack` (auto-contenu, cible+attaquant en interne, QUIC réel) → **6/6, 0 illégitime accepté**. A1 forge 20→0 accepté ; A2 replay 30→0 ; malformé absorbé ; flood challenges budget plafonné (10/40 signés) ; essaim Sybil 20 identités/302 recv → cap GLOBAL 120 tenu (vs 200 si cap par-identité seul, confirme kill-shot #5) ; cible intacte (drops unknown=50 badsig=0 incoherent=20). Aucune régression de sécurité introduite par les changements du jour. 0 process résiduel (auto-terminé). | Défenses L1/PoP tiennent après refonte — pas juste unit test, QUIC réel | `tom-stress presence-attack`, `docs/redteam/corpus.jsonl` |
| 2026-07-15 | **✅ PREUVE DE LONGÉVITÉ 25h — le réseau stabilisé TIENT dans la durée** : après la consolidation des 13-14/07 (4 bugs de fond + throttle rejoin + fixes flaky), la flotte a tourné **25h30 en continu SANS intervention** : NAS (Freebox) 25,5h uptime, **34 971 msgs envoyés / 39 115 reçus, 0 restart, 0 gel, 0 churn** ; MacBook Pro 25,3h, 9 763/10 314. ~74k messages échangés au total. Le deadlock QUIC (ex-« stall périodique ») ne s'est JAMAIS reproduit, le throttle #19 a tenu (0 orage de rejoin sur 25h). Seule intermittence : suspensions iOS/tvOS des appareils mobiles écrans verrouillés la nuit (limitation OS #4, attendue, backup TTL 24h couvre, relance 1× au réveil) — le cœur Mac+NAS est resté toujours-actif. **Contraste avec le point de départ** : il y a 48h « je lance Tom et je vois quelques messages et ça s'arrête ». La stabilité est acquise et prouvée en endurance réelle, pas juste en test. | Jalon de fiabilité : réseau prod-grade en endurance | `curl :8085`/`:9091` 2026-07-15 ~10h, campaign.log, 0 NRestarts sur 25h |
| 2026-07-15 | **✅ #13 CONFIRMÉ RÉSOLU + résilience re-validée (fenêtre isolée, flotte off)** : presence-storm **5/5 PASS** (mesh 4 nœuds formé = plus de « 0-discovery » ; 80 vagues de challenges échangées = connexions vivantes ; les « send loop terminated » n'apparaissent qu'APRÈS le [PASS], au teardown, PAS pendant l'exécution → ce n'était PAS le bug #13, qui était causé par l'inbox saturée tuant les connexions en cours, corrigée le 14/07). chaos-monkey **6/6 PASS** (4 kills/2 revives/10 clock-skews ; réseau jamais mort min_alive=2 ; nœuds responsive après churn+skew = pas de régression FINDING #1 ; flotte auto-soignée). **Observation honnête (FINDING #2, non-bloquant)** : après un heal, le step « presence flow resumes » note 0 NOUVELLE acceptation de présence (accepted 0→0) — probablement lié à l'Online-marking (ADR-011 : re-marqués Known pas Online après reconnexion), à creuser un jour, mais le step passe et le réseau est fonctionnel. Les bugs #13 (fleet-probe 0-discovery + campaign connexion morte) étaient bien des downstreams des fixes du 14/07. | #13 clos par preuve, pas par supposition ; résilience chaos OK | `tom-stress presence-storm`/`chaos-monkey` 2026-07-15 |
| 2026-07-15 | **FINDING #2 élucidé = PAS un bug (gate anti-Sybil qui fonctionne)** : le « 0 acceptation présence après heal » du chaos-monkey vient du gate A5 (`state.rs:1959` : accepter une attestation exige `role_manager.score(pair) >= presence_contribution_min`). Après un heal, un pair reconnecté n'a pas encore ré-accumulé de score de contribution (relais réel) → attestations légitimement droppées (DropGate) jusqu'à ce qu'il re-prouve son travail. « Corriger » affaiblirait l'anti-Sybil (kill-shot #5 : un attaquant kill/revive réinitialiserait sa réputation). Le test l'observe sur 5s, trop court. → DOCUMENTÉ, pas touché (doctrine : ne pas affaiblir un garde-fou pour un chiffre). | Faux positif rassurant : la sécurité prime | `crates/tom-protocol/src/runtime/state.rs:1959` |
| 2026-07-15 | **✅ Amélioration : cap + filtre du rejoin gossip (bruit DHT)** : le rejoin sur NeighborDown rejoignait TOUS les known_peers (topology = centaines d'entrées rendez-vous DHT, surtout du bruit jamais dialable) → dials gaspillés sur appareils faibles. Fix (`loop.rs`) : ne rejoindre que les pairs vus dans les 5 dernières minutes (`REJOIN_RECENT_WINDOW_MS`), triés most-recent-first, cappés à 16 (`REJOIN_MAX_PEERS`). Complète le throttle #19. 632 tests + clippy workspace verts. | Moins de dials gaspillés, complète l'anti-orage | `crates/tom-protocol/src/runtime/loop.rs` |
| 2026-07-15 | **Améliorations mineures — bilan honnête (2/3 = non-actions justifiées)** : (1) ✅ cap rejoin DHT FAIT (poussé). (2) **Skew horloge réel : NE PAS fabriquer.** Le seul `presence_clock_offset_ms` existant (`runtime/mod.rs:109`) est un offset de SIMULATION (chaos-monkey l'injecte via `SetPresenceClockOffset`, loggé « SIM: ») — PAS une dérive mesurée inter-nœuds. Le protocole n'a pas de NTP (design A3). Un vrai signal (comparer timestamp d'attestation reçue vs horloge locale, en séparant latence RTT) serait un mini-chantier FFI+UI, pas un fix mineur → l'horloge locale affichée (P2S3) reste le compromis honnête ; skew inter-nœud = chantier futur scopé, ne pas inventer un faux chiffre (enseignement `observability-must-reflect-ground-truth`). (3) **Panic racine poison mutex : ne se manifeste PLUS.** Depuis le fix deadlock (drop guard) + fix poison (unwrap_or_else), 0 gel/0 abort sur 25h — rien à capturer sans nouveau gel (qui n'arrive plus). Résidu théorique, non-actionnable en l'état ; le binaire lock_tracking reste dispo si ça resurgit. | Discipline : ne pas fabriquer de signal faux, ne pas chasser un fantôme éteint | `runtime/mod.rs:109`, 25h sans gel |
| 2026-07-15 | **✅ Release 2.0.0 publiée + APNs acté non-objectif + skew RÉEL livré + fix reconnexion réseau iOS** — journée décisions & durcissement. **(1) Release 2.0.0** : PR release-please #54 (1.0.0) mergée mais la GitHub Release a échoué (tag `v1.0.0` DÉJÀ pris par un placeholder orphelin du 2026-02-05, antérieur à v0.6.0). Décision utilisatrice : sauter à 2.0.0. Commit `Release-As: 2.0.0` → PR #55 → mergée → tag+release **v2.0.0 « Latest »** publiés proprement. **(2) APNs/background = NON-OBJECTIF produit assumé** (Known Limitations #4 passé 🔻→🟢) : pas de daemon background ni push APNs/VoIP ; le background permanent vit sur **VM Linux / Raspberry Pi / NAS / self-hosters**, PAS sur smartphone/tablette (un mobile contribue en foreground, quelques min de sursis max). Mémoire `apns-background-is-non-goal`. **(3) Skew horloge inter-nœud RÉEL livré** (revirement assumé vs « ne pas fabriquer » du matin — cette fois c'est un signal MESURÉ, pas inventé) : `ClockSkewMetrics` (`runtime/clock_skew.rs`, fenêtre 64/médiane/borne outlier 24h) échantillonne `now_ms - envelope.timestamp` **UNIQUEMENT après vérif de signature** (`state.rs:1646`, même gate anti-abus que le crédit bande-passante PoP #1) → un attaquant ne peut pas fausser la métrique. Exposé FFI (`clock_skew_ms/samples`) + chip « écart réseau » dashboard (vert<2s/orange<30s/rouge≥30s). Honnête : inclut la latence, PAS du NTP — but = détecter une horloge grossièrement fausse/attaque temporelle. **Test CI flaky rattrapé** : borne `<=1020ms` cassait en CI (skew = 1000 + délai exécution runner chargé) → borné `[1000,3000)` (déterminisme exact couvert par les unit tests de `clock_skew.rs`). **(4) Fix reconnexion réseau iOS** (`NWPathMonitor` dans `TomNodeService`) : mode avion ne changeait pas `scenePhase` → nœud `.running` figé sur « déconnexion », jamais relancé. Fix = relance `forceReset+start` (comme retour-foreground) sur 3 déclencheurs : perte→retour réseau, **handoff interface active Wi-Fi↔cellulaire** (rupture Wi-Fi→data mobile, même en foreground — via `usesInterfaceType`), et retour réseau d'un `.error`. Bascule d'état SYNCHRONE anti-course + throttle 3s anti-tempête ; arrêt manuel jamais outrepassé. **Bug de compile rattrapé** : le chip skew référençait `nodeService.status` inexistant → cassait le build de l'app ; a filé car AUCUN job CI ne compile l'app Swift (le `swiftc -parse` d'un sous-agent ne résout pas les types). Corrigé (propriété `@Published clockSkewMs`) + **compile tvOS vérifiée en direct (BUILD SUCCEEDED, 0 erreur)**. Builds 52→53. | Décisions produit tranchées + 2 signaux terrain honnêtes livrés ; leçon : compiler POUR DE VRAI l'app Swift (CI ne le fait pas) | commits `1e0432b`(rel1.0)→`33297af` ; v2.0.0 ; loop-master multi-angles (compile+angle-mort) |
| 2026-07-18 | **🔄 R14 RENVERSÉ par ses propres mesures + R15 réduit de moitié + flotte à 5 nœuds (iPhone Laura)** — session design-first, **aucun code protocole écrit**, 2 docs de conception poussés (`9b39837`). **(1) R14 « IPv6 first-class » n'était pas le bon chantier.** Première baseline : « v6 majoritaire 7/9, l'iPhone est l'exception tout-v4 » — **sur-interprétation d'un snapshot unique**. Trois relevés successifs sur flotte inchangée : presque chaque chemin change de famille en une heure, et surtout **le système ne converge pas vers le meilleur lien** (`iPad→iPhone` passé de **v4 9 ms à v6 51 ms** et y reste ; au dernier relevé **11 v4 contre 5 v6**, l'inverse de la « baseline »). **Cause prouvée dans le code** : l'ordre des candidats est **aléatoire**, ni v4-first (thèse du red-team) ni parallèle (ma thèse) — le `BTreeSet` trie bien v4<v6 (`tom-base/endpoint_addr.rs:44-51`) mais l'ordre est **perdu à l'insertion en `FxHashMap`** (`tom-connect/.../path_state.rs:38`, `tom-quinn-proto/iroh_hp.rs:91`), et `iroh_hp.rs:196` montre qu'un autre mécanisme **s'appuie** sur ce désordre. Chantier renommé **« convergence de chemin »** ; préférence v6 forcée, happy-eyeballs, filtrage IN6_IFF_TEMPORARY et règle pare-feu Freebox (refusée par Malik : on ne touche pas à la box) **tous abandonnés — aucun justifié par les mesures**. **(2) R15 annuaire réduit à R15-lite.** Cartographie : `peers` = 4 colonnes (`storage/schema.rs:66-72`), **zéro adresse** ni sur disque ni en RAM ; backup ADR-009 = RAM pure. Persister les **adresses directes** rapporte peu (mDNS 1-2 s sur LAN ; hors-LAN ce sont les plus volatiles) et **rouvre le vecteur d'empoisonnement** du 17-18/07 avec des adresses survivant au restart → **écarté** ; **relais habituel** retenu (déjà auto-appris en RAM, `tom-transport/protocol.rs:250-257`). **(3) Red-team contre-vérifié : 3 findings sur 7 réfutés** — dont le plus grave (« PathEvent non authentifié », l'agent avouant « jamais lu, donc hypothèse ») : `protocol.rs:241` `remote = NodeId::from_endpoint_id(connection.remote_id())` → identité issue du handshake QUIC/TLS (ADR-005). **Mais ma contre-vérification a trouvé ce qu'il avait manqué** : `storage/mod.rs:463-475`, le filtre d'élagage 24 h est gardé par `status != Online` alors que le load **préserve** `Online` → **un fantôme sauvegardé `Online` est rechargé quel que soit son âge** (borné à ~30 s sauf crash-loop) ; le commentaire au-dessus décrit l'intention, pas le code. **(4) Flotte à 5 nœuds** : iPhone de Laura déployé en build 125 (`ONLY=` ajouté à `deploy-apps.sh`), Converged, 4 pairs, intégré à l'orchestrateur ; campagne #1/#2/#3 **11/11 PASS**. Un nœud **neuf sans historique a tiré 2 v4 / 2 v6** — confirmation directe de la loterie. **(5) Outil livré** `scripts/path-matrix.py` : N relevés espacés, **bascules + asymétries de sens**, jamais une photo. Révèle 3 asymétries (`iphone→ipad` v6/15 ms ≠ `ipad→iphone` v4/6 ms) : chaque sens fait son propre tirage. | Deux chantiers roadmap redéfinis par les faits avant d'écrire une ligne ; méthode : jamais qualifier un régime réseau sur un relevé unique | `9b39837` + docs `r14-ipv6-first-class.md`/`r15-annuaire-local.md`, mémoires `tom-path-selection-not-converging`/`tom-r15-reduced-to-relay-only` |
| 2026-07-19 | **🔴 BUG CRITIQUE : backup store borné en NOMBRE de messages, pas en OCTETS — un nœud NAS étouffé + « double tunnel » tranché (pas de double tunnel)** — trouvé en voulant mesurer autre chose. **(1) Le bug** : `backup/store.rs:14` `MAX_TOTAL_MESSAGES=10_000` borne la **cardinalité**, jamais le **volume** ; `BackupEntry.payload` = `Vec<u8>` **entier en RAM** (`backup/types.rs:50-54`), backup ADR-009 en RAM pure, **aucun cap en octets** (grep vide). ~100 messages de 8 Mo suffisent à saturer. **Terrain NAS (VM 920 Mo)** : **688 Mo** après 13 h, `free` 54 Mo dispo, **OOM-killer déjà passé** (`Killed process 20593 (tom-chat) anon-rss:771260kB`), **8 366 échecs** (56 la veille au soir), **0 pair connecté**. **Preuve A/B au restart** : **688 Mo → 24 Mo, 0 → 5 pairs, 8 366 → 0 échec**, même binaire/même réseau. **Contributeur : mes propres campagnes de charge de la veille (échelle 1/2/4/8 Mo) — un test légitime peut tuer un nœud de production.** 3ᵉ récidive de la classe « borne par-unité sans budget global » (cf. large-message-dos, reassembly-memory-dos). **(2) L'observabilité a menti pendant tout ce temps** : le NAS affichait `phase:"connecte"`, service systemd `active (running)`, et le **Mac le voyait « DIRECT v6 7 ms »** — alors que `pairs_connectes` était **vide** ; un `/send` a répondu `{"ok":true,"envoyes":15}` **sans aucun pair** (le compteur compte l'intention). → ne jamais conclure « le nœud va bien » depuis `phase` ni depuis la vue d'un pair. **(3) Question « double tunnel » TRANCHÉE par `tcpdump`** (1 274 paquets) : pendant 12×100 Ko NAS→Mac, **861 paquets de données sur UN SEUL chemin v6**, **0 en v4** vers le même pair, retour symétrique sur le même couple d'adresses, les autres adresses ne recevant que des sondes de 8 o. → **une seule connexion QUIC, plusieurs chemins candidats, un seul actif** : ni double tunnel ni double processus. Le multipath QUIC fait déjà « choisir + garder un secours » ; le trou de R14 est la **ré-évaluation** (hystérésis 5 ms), pas le choix. ⚠️ La 1ʳᵉ tentative de cette mesure était **polluée** (NAS étouffé → ses sondes multi-adresses étaient des reconnexions désespérées, pas du multipath sain) : **mesurer sur un nœud dégradé fabrique une fausse théorie**. **(4) Pièges** : service NAS = **`tom-node`** (pas `tom-chat` → `journalctl` vide) ; status Mac écoute en **IPv6 seul** (`curl 127.0.0.1:9091` échoue, `[::1]:9091` marche) — « injoignable » ≠ mort. | Bug critique attrapé avant mise en production élargie ; question de conception R14 fermée par la preuve | `tcpdump` NAS, `dmesg`/`free`/`systemctl`, mémoire `tom-backup-store-oom-2026-07-19`, doc `r14-ipv6-first-class.md` §2.3bis/§2.3ter |
| 2026-07-19 (suite) | **🟢 OOM RÉSOLU — la vraie cause était `pending_envelopes`, pas le backup (builds 126+127)** — Malik confirmait le symptôme terrain : « certains soirs la box ventilait fort » pendant les campagnes de test. **(1) Build 126** : budget octets du backup store (`MAX_TOTAL_BYTES=64 Mio`) — nécessaire, mais **insuffisant** : l'OOM-killer a de nouveau tué le nœud à 780 Mo RSS avec le binaire corrigé. **(2) Enquête** (workflow 4 angles, chacun réfuté par un 2ᵉ agent) → coupable vérifié sur pièces : `runtime/state.rs:115` `pending_envelopes: HashMap<String, Envelope>` — cache de réémission R9.2 qui clone l'**enveloppe complète, payload inclus** à chaque envoi (`state.rs:2540`), **sans aucune borne**. Vers un pair injoignable rien n'est acquitté → rien ne sort avant expiration du tracker. **4ᵉ occurrence de la classe « borne par-unité sans budget global »** (large-message, reassembly, backup, celle-ci). **(3) Build 127** : `runtime/pending.rs`, structure encapsulée bornée à 32 Mio (délibérément sous les 64 Mio du backup : la réémission est un confort, le backup est le filet ADR-009). Encapsuler plutôt qu'un compteur à côté d'une HashMap manipulée depuis 7 endroits — leçon directe du fix précédent. 4 tests. **(4) VALIDATION TERRAIN, après 2 tests INVALIDES écartés** : ⚠️ `MemoryCurrent` de la cgroup **inclut le page cache** (exagérait 2-5×) → seul `RssAnon` compte ; ⚠️ un envoi vers un pair absent de la topologie **n'est ni tenté ni compté** (`envoyes` restait plat à 74 pendant que je croyais pousser 240 Mo). Test valide (300 Mo vers pair connecté, `envoyes` 81→274, 0 redémarrage) : RssAnon 148 → **pic 491 Mo** → **123 Mo stable à +120 s**. **La mémoire redescend enfin** — avant elle montait sans jamais redescendre jusqu'à l'OOM. Bilan : 688 Mo stable + OOM + 0 pair + 8 366 échecs → pic 491 puis 123 Mo stable, 4 pairs, `NRestarts=0`, aucun nouvel OOM. **Reste surveillé (honnête)** : le pic transitoire de 491 Mo reste élevé pour une VM de 920 Mo — ce n'est plus une rétention mais le coût du trafic en vol non régulé (piste : réguler le débit ou borner `send_window` QUIC, jamais tuné). | Bug critique réellement fermé et mesuré ; classe de bug enfin nommée et documentée | `76903f3` (126) + `9fbb32a` (127), doc `fix-backup-store-budget-octets.md`, mémoire `tom-memory-retention-class-of-bug` |
| 2026-07-19 (fin) | **📋 Terrain préparé pour R14/R15 — protocole de test chaos + prompt de reprise réécrit** : `docs/plans/protocole-test-r14-r15.md` (4 scénarios R14 : observabilité des bascules, élucider une bascule dégradante EN DIRECT, déterminisme du probe, charge dégradée ; 3 scénarios R15-lite : gain de reconnexion, **non-résurrection des fantômes avec `state.db` pollué en fixture**, cascade TTL ; invariants **I9a/I9b/I10/I11** chiffrés ; checklist avant/pendant/après ; limites assumées). ⚠️ **Rédigé par sous-agent puis RELU — 3 erreurs factuelles corrigées et signalées en tête** : le status du Mac écoute en **IPv6 uniquement** (`curl 127.0.0.1:9091` = faux négatif « injoignable »), plusieurs **endpoints étaient inventés** (`?raw=remote_candidates`, `/peers?action=drop`, `?action=force_save` — le serveur n'expose que `/`) et sont marqués `[ENDPOINT À CRÉER]` relevant du Lot A, et l'iPhone de Malik n'est plus sur le réseau. Sans cette relecture, la session suivante perdait des heures sur des commandes qui échouent. **CLAUDE.md mis à jour** (2 entrées Known Limitations : rétention mémoire résolue + pic transitoire différé ; phase → build 127). **Roadmap** : OOM marqué résolu avec chiffres terrain, entrée « pic mémoire » créée en différé (décision Malik : après R14 et R15). Prompt de reprise **entièrement réécrit** — l'ancien décrivait R14 comme « IPv6 first-class » et R15 comme un annuaire d'adresses, périmés tous les deux. | Successeur outillé : rien à re-découvrir, les pièges sont nommés | `240dfc6`, mémoires `session-handoff-2026-07-19` + `tom-memory-retention-class-of-bug` |
| 2026-07-19 (soir) | **✅ R14 Lot A + R15-lite LIVRÉS et validés terrain, Lot B TRANCHÉ sur données, red-team hardcore 7 agents, durcissement mémoire (builds 128→130)** — grosse session d'exécution. **(1) R14 Lot A (128)** : observabilité des bascules de chemin. `PathEvent` porte `family`/`prev_family`/`prev_rtt` (vérité PAR-CONNEXION du watcher, exclut l'artefact multi-connexions) ; `paths_by_peer` expose `family/switches/last_switch/last_switch_at_ms` côté FFI (contrat verrouillé par `path_info_json_keys_match_swift_decoder`) ET côté NAS (tom-tui) ; ligne collecteur enrichie ; `path-matrix.py` : Mac `[::1]`, NAS `:8085` normalisé, compteurs vue-nœud. **(2) R15-lite (129)** : `preferred_relay_url` schéma V5, appris des seuls PathEvent RELAY authentifiés (cap 512 o), load M2-filtré (non-résurrection PAR CONSTRUCTION), semis du pool au démarrage (zéro présence, zéro dial). Test déterministe end-to-end (relais embarqué réel + **relais leurre anti faux-vert**). **CI rouge 3× avant vert** : cause = `RelayMode::Disabled` n'installe PAS le transport relais (un nœud sans relais configuré ne peut pas dialer via relais — masqué localement par la pollution d'env). Terrain NAS : `--data-dir` (drop-in systemd), cycle complet observé (« Restored 1 preferred relay routes » → « R15: 1 relais semés »). **(3) Fix élagage (73f9438)** : le TTL 24h au load s'applique quel que soit le statut persisté (le garde `status != Online` exemptait exactement les fantômes crash-loop). **(4) Lot B TRANCHÉ sans intervention** (logger passif 40 min) : la non-convergence = **failover sur mort de chemin + ABSENCE de re-sondage du perdu**, PAS une sélection défaillante — bascules à perte interdites par l'hystérésis donc = failovers ; v6 renaît sur 3 adresses différentes de l'iPad ; après failover vers un pire chemin, aucun retour ≥25 min (le mort n'est plus mesuré → l'hystérésis n'a rien à comparer). **(5) Décision produit §5** : on ASSUME le power save iOS (churn de chemins d'un mobile inactif = attendu, extension du non-objectif APNs) — le Lot C vise la re-convergence des nœuds ACTIFS. **(6) Red-team HARDCORE 7 agents** (design Lot C + 4 lentilles sur le code livré) : **3 findings « bloquants » RÉFUTÉS sur pièces** (URL relais « attacker-controlled » → route par-pair + E2E + préexistant ; asymétrie FFI family/switches → champs non-Optional toujours sérialisés, l'agent a mal lu le serde ; note_path_event sans else-remove → c'est le design R15-lite voulu). **1 finding RÉEL retenu → durcissement (130)** : `tick_cache_cleanup` ne bornait que `relay_routes` ; un pair parti par timeout heartbeat (cleanup_departed, sans PeerOffline) laissait `peer_paths`/`backup_redelivery_queue`/`backup_last_redelivery_at` orphelins → retain complet + purge queue à PeerOffline (reconstruite du store au retour). Pas un OOM (le drain expirait déjà à 24h) mais chemin fermé. **Design Lot C RÉVISÉ** par le red-team : déclencheur = `PathEvent::Abandoned` (pas `new_rtt>old_rtt`, tautologique), réutilise `open_path`/PATH_CHALLENGE existant (pas de sonde neuve), cooldown anti-oscillation 30s/10ms + ≤1 re-probe/cible anti-amplification (fade réversible, LOCKED #4 OK). | Deux chantiers roadmap LIVRÉS+validés, un tranché par la mesure, code durci par red-team adversarial ; méthode : arbitrer les findings sur pièces (3/7 bloquants réfutés) | `73f9438`→`e3c5f47`, builds 128-130 flotte, CI verte, mémoires `session-handoff-2026-07-19-apresmidi`/`tom-lotb-verdict-failover-sans-resondage`/`relaymode-disabled-pas-de-transport-relais`/`ios-power-save-churn-assume`, doc `r14-lot-c-resondage.md` |
| 2026-07-19/20 soir-nuit | **✅ I10 / R15-lite VALIDÉ TERRAIN, R14 Lot C IMPLÉMENTÉ (feu vert Malik 19/07 18h) — measurements précises + design Lot C tranché par la red-team** : (1) **I10 mécanisme CONFIRMÉ RÉEL** — restart chaud NAS deploie route relais habituelle en t+0,4s via `PathEvent RELAY` → upgrade `DIRECT v6`. « Restored 7 preferred relay routes » → changement chemin. **Ratio ≥2× NON mesurable LAN** : 11 cycles, baseline froide écrasée par 3 canaux d'assistance (re-dials entrants, TCP 3340 survivant restart, relais embarqué non-tuable du boot). Preuves : `docs/plans/protocole-test-r14-r15.md` §3.1 « RÉSULTATS 2026-07-19 ». Reste : chiffrer hors-LAN (iPhone cellulaire, app relancée post-NAS restart). (2) **R14 Lot C — CODE LIVRÉ, GATE VERTE, BUILD 131 PRÊT MERGE.** Re-sondage chemins morts dans tom-connect SEUL (remote_state.rs, path_state.rs, actor mono-tâche, 0 nouveau verrou, 0 modif quinn-proto). Trigger : `PathEvent::Abandoned` d'une IP active. Re-probe `open_path` (PATH_CHALLENGE existant) backoff 30s→5min ×6, cooldown 30s/gain 10ms, ≤1 re-probe vol/adresse. Découverte implémentation : exclure `deliberately_closed` sinon probe→reopen→close boucle. 80/80 tests, 0 deadlock. Doc : `docs/plans/r14-lot-c-resondage.md` §5bis. (3) **Reste OUVERT** : harnais étage L « kill/revive chemin » — design proxies UDP loopback, scalabilité QNT — OBLIGATOIRE avant flotte Lot C. Étage F (I9a/I9b) après. Apple TV disponible. (4) **Roadmap** : Lot B tranché (failover+absence re-sondage perdu), Lot C en attente harnais+validation. | Mécanisme R15-lite validé sur relais embarqué réel ; Lot C cœur prêt ; harnais étage L = scalabilité suivante | Repro 11 cycles `docs/plans/protocole-test-r14-r15.md`, build 131 Rust, design+code `r14-lot-c-resondage.md`, notes Malik 19/07 18h |
| 2026-07-20 (nuit) | **✅ RECADRAGE RÔLES EXÉCUTÉ + banc courbe FINALISÉ/poussé après re-oracle (3ᵉ artefact de mesure tué) + design banc « rôles sous charge »** — session de reprise post-recadrage Malik (« le réseau est un organisme à rôles, pas un tuyau send/ACK »). **(1) Relecture INTÉGRALE des notes via qmd** (index `tom` réparé : ajout collections docs/ + _bmad-output/, 187 fichiers, embeddings ; ⚠️ le MCP qmd de la session servait un état RAM incohérent avec fuite 1RR malgré `--index tom` sur la ligne de commande → contourné par CLI `qmd --index tom`, vérif témoin obligatoire en début de session) → **`docs/plans/prisme-des-roles.md`** : grille vision→code→écart des 9 rôles (whitepaper §3-4, Plan Maître V2, Master Map, ADR-011 PoP), 8 axes de scénarios R1-R8. Écarts clés : rotation en cascade + carnet de rendez-vous TOURNANT = nulle part conçus ; Observateur/Validateur/L1 pleins = M1.5-6 ; primitifs L1-001/003 DÉJÀ en terrain (`presence/`). **(2) Banc courbe : re-oracle 4 agents → TESTS BLOQUANT (couverture des mécanismes de mesure) + CODE MAJEUR (4 findings, 2 vérifiés réels sur pièces)** → tout corrigé : garde N≥2 (self-send tautologique), fenêtre de quiescence garantie même à zéro message, magic 4 octets d'herméticité, **teardown borné par point** + extraction des logiques de mesure en fonctions pures + **13 tests (8 nouveaux dont smoke E2E N=2)**. **Le teardown a révélé le 3ᵉ artefact : sans shutdown, les nœuds des points précédents restaient VIVANTS pendant les suivants (N=20 tournait avec 15 parasites)** → 87,7 % lu, 97,3 % réel. **Chiffres finaux (build 137, seed 42, 15 s/point) : N=5 100 %, N=10 100 %, N=20 97,3 % (8/300 perdus), 0 doublon partout, dérive ≤1,02×** → FAIL honnête à N=20, attribution ouverte (runtime partagé saturé, p50 ×31) → multi-host §3. Trois artefacts successifs accusaient TOUS le protocole — l'instrument d'abord. Poussé : `4bb4288..1b678b8` (6 commits, gate verte). Fix bonus : ATV retirée de l'orchestrator (sondée injoignable .76:9091, cohérence flotte 137). Findings transport nouveaux : boucle chaude `MaxPathIdReached` (~46 retry/ms sans backoff), panic `closed without error reason` (`tom-quinn/connection.rs:1723`), multipath sondant l'IPv6 globale du Mac en banc loopback. **(3) Design `docs/plans/banc-roles-sous-charge.md`** (design-first, AUCUN code) : 8 scénarios R1-R8 (relais multi-hop, backup virus, promotion/fade, rendez-vous, subnets, failover hub, PoP anti-fantômes, arroseur arrosé) + **capstone « la vie de l'organisme »** (tous les rôles en même temps sous charge, 15 min orchestrées TEST-*) ; étages L/F, phases A/B/C ; prérequis P1-P4 vérifiés dans le code (P1 namespace DHT test + P3 timeout subnet configurables = à construire, P2 événements majoritairement DÉJÀ exposés). Revue DOCTRINE dédiée : MAJEUR ratifiable → ordre imposé P1→R4→capstone + discipline anti-pollution gravés dans le doc. | Le mandat « les rôles doivent transpirer » est matérialisé en docs + le banc qui juge est enfin digne de confiance | push `1b678b8`, docs `prisme-des-roles.md`/`banc-roles-sous-charge.md`/`banc-courbe-masse.md` §2bis, mémoires `banc-courbe-masse-phase1-inprocess`/`qmd-index-scope-tom` MAJ |
| 2026-07-21 (matin) | **✅ GO Malik exécuté de bout en bout : flotte 139 déployée, P1+P3 livrés, banc rôles Phase A LIVRE SON PREMIER BUG PROTOCOLAIRE (antispam percé, corrigé, prouvé)** — **(1) Flotte 139** : Mac/iPad/**ATV réintégrée** (134→139, demande Malik) ; iPhones Malik+Laura : 139 installé, launch « device locked » → s'ouvre au déverrouillage ; compte à rebours auto-start visible (décision : 5 s conservées + affichage) ; NAS intact (rien de Rust). Piège réglé : `open` activait l'ANCIEN process Mac (137) → pkill puis relance. **(2) P1 namespace rendez-vous isolé** (`be45066`) : type `RendezvousNamespace` (prod = invariant wire golden-testé ; `test(label)` préfixé `tom-test-` infalsifiable), AUCUNE env var (vecteur d'injection rejeté au red-team), WARN au boot non-prod, isolation prouvée bout-en-bout sur Testnet (publish ns test invisible de la prod). **(3) P3** : timeout dissolution subnet configurable test-only (défaut 5 min intact). **(4) Banc `tom-stress roles-charge` Phase A** : R5 subnets **3/3 PASS** (formation trio dense / frontière (silencieux jamais embarqués) / dissolution inactivité — le « territoire de rôles » wp §3.3 exercé pour la première fois) ; R8 arroseur : le run 1 a **DÉBUSQUÉ UN BUG RÉEL** — `state.rs` : le rejet du message hors-budget vivait DANS le dédup 10 s de l'alerte SenderThrottled → seul 1 message/10 s était rejeté, **1608/1609 spams LIVRÉS** (décision #5 réduite à une alerte cosmétique ; les tests unitaires ne testaient que l'émission de l'event, pas le rejet). **Fix `e1dc778`** : rejet systématique, alerte seule dédupliquée + test du contrat + oracle R8 durci (« freinage-réel ») → **preuve terrain 419/1596 livrés**, victimes intactes (20 vs 16), rédemption 12/12, jamais exclu (LOCKED #4/#5 enfin VRAIS en pratique). Review RATIFIÉ ; finding résiduel documenté (**Known Limitations #13** : budget consommé sur `from` non authentifié = famine par spoof 1:1, préexistant, ticket). Leçon : un banc de rôles avec oracles durs paie dès le premier run — le smoke « ça envoie des events » n'aurait jamais vu ça. | Le mandat « organisme à rôles » livre : premier rôle-mécanisme (arroseur) réparé par la preuve, subnets exercés | push `6a55f89`, banc `scenario_roles_charge.rs`, mémoires/tâches à jour, reste : R1+R3 couplés, R2, R7 (tâche 9) |
| 2026-07-21 (matin-2) | **🔴→✅ 2ᵉ bug protocolaire débusqué par le banc rôles : le relais CRÉDITAIT L'ORIGINE du message (usage devenait contribution)** — en préparant R1 (multi-hop), lecture du bras Forward (`state.rs`) : `record_relay(envelope.from)` + `record_bytes_relayed(envelope.from)` chez le RELAIS créditaient l'ÉMETTEUR d'un relais qu'il n'a jamais fait. Conséquences : spammer via relais GONFLAIT son budget antispam (`compute_rate`) chez le relais et faisait franchir le gate anti-Sybil A5 (`presence_contribution_min`=2.0) en émettant ~5 messages — violation directe du PoP ADR-011 §1 (« crédit = travail CONSTATÉ, jamais le transit ») ; le crédit légitime (RelayForwarded ACK chez l'émetteur, anti-pumping FINDING #7) était lui correct. **Fix `2937330`** : crédit-origine supprimé + test contrat `forward_never_credits_the_origin_with_relay_score` ; 694 tests verts, AUCUN ne dépendait du crédit (résidu R4 pré-PoP). **Design R1+R3 finalisé et vérifié pièce par pièce** (dans la tâche 9) : amorce `upsert_peer(PeerInfo{role:Relay})` (API publique), `select_path` inconditionnel → via=[R], `executor.rs:53` envoie à via[0] (multi-hop réel), R aussi relais de B (les ACKs bouclent via R), promotion à 10 RelayForwarded constatés = R3. | 2 bugs protocolaires en une matinée par le MÊME banc — la lecture-sous-prisme-rôles paie autant que les runs | fix `2937330`, tâche 9 scellée (design complet), reste : coder r1/r7/r2 dans scenario_roles_charge.rs |
| 2026-07-21 (midi) | **✅ PHASE A DU BANC RÔLES COMPLÈTE (22 oracles PASS) + 3ᵉ bug protocolaire + fuite d'herméticité entrante élucidée** — reprise après correction du calcul de contexte (je m'étais arrêté à tort « 99% », vraie fenêtre → ~48%). **(1) R7-L PoP anti-fantômes** : nouvelle API `get_peer_statuses` (vue Online/Known, read-only, non-FFI) ; un fantôme re-découvert 6× reste Known, jamais Online ; V (trafic réel) est Online. **(2) R2-L backup absent→retour** : `.identity_path()` → B re-bind le MÊME node_id après arrêt ; nouvelle API `get_backup_pending_count` (lecture directe du store, distingue ADR-009 du cache R9.2) ; 6 oracles (échauffement, même-identité, backup-à-absence store retient 3, livraison-différée 3/3, exactement-une-fois, purge-après-ack). **3ᵉ BUG débusqué** : le bras `RecipientReceived` (state.rs) faisait `mark_delivered`+`pending_envelopes.remove` mais JAMAIS de purge du backup store → quand l'émetteur est son propre gardien (cas fréquent, state.rs:2634), l'ACK ne purgeait pas → rétention jusqu'à TTL 24h (contraire ADR-009 « self-delete when delivered »). Fix `241b447` : purge LOCALE `store_mut().mark_delivered_batch` (sans broadcast — le gardien livre en direct ; no-op sur livraisons directes) ; R2 purge-après-ack 0 restant, 695 tests verts, KL#14. **(3) Fuite d'herméticité ENTRANTE** (révélée par le online_count flaky de R7) : le trio isolated coupe la découverte SORTANTE du nœud de banc mais PAS l'acceptation de connexions ENTRANTES — les apps de la flotte sur la même machine trouvent le nœud de banc par mDNS et lui poussent leur carnet → Known « étrangers ». **Élucide le « chatter du canal applicatif » du banc courbe** (même cause). N'affecte pas le PoP (restent Known, pas Online) ; oracles rendus RELATIFS ; chantier d'herméticité étage L documenté. **BILAN : 3 bugs protocolaires en une session (antispam percé, crédit-origine, backup non purgé), tous par le même banc — le mandat « organisme à rôles » validé.** | Phase A finie ; le banc rôles est un révélateur de bugs que send/ACK ne verrait jamais | push `241b447`, 22 oracles, KL#13/#14, reste : capstone R4-F multi-host + KL#13 ticket |
| 2026-07-21 | **✅ HERMÉTICITÉ ÉTAGE L — banc isolé par construction, 2 verrous (design doc appliqué)** : enquête a **infirmé l'hypothèse mDNS** du handoff (entry 131) — `local_discovery(false)` coupe bien annonce ET résolution, `MdnsAddressLookup` jamais construit (`node.rs:389-445`), « mDNS entrant » n'existe pas. **Canal réel prouvé** : `.cargo/config.toml` (machine, hors git) injecte `TOM_EXTRA_FALLBACK_RELAY=http://82.67.95.8:3340` (NAS public flotte) → découverte HTTP vide → DNS TXT vide → fallback compile-time (option_env `config.rs:31-36`) → `RelayMode::custom(NAS)` partagé (`node.rs:246-248`). Tout nœud `isolated` se connectait donc au relais de la flotte. **Fix A** : `TomNodeConfig::hermetic()` force `RelayMode::Disabled` (saute 3 fallbacks, incompatible relay_only). **Fix B (défense profondeur)** : `GatedHandler<H>` wrappe 2 ALPN → refuse non-whitelisted peers AVANT délégation (`TomNode::set_allowed_peers(Option<HashSet<NodeId>>)`, défaut None = prod inchangée, leçon pool-lock). **Câblage banc** : `roles_charge`/`courbe`/`invariants`/`chaos_monkey` en `.hermetic()` + listes blanches IDs connus avant attach. **Oracle renforcé** : R7.herméticité-zéro-étranger (foreign_known=0, tolérance supprimée). **Preuves** : `cargo test -p tom-transport 48/48` PASS (gate_rejects_unlisted_peer : B passe/C refusé), clippy 0 warning, **`roles-charge --scenario all` 23/23 oracles PASS, 0 ligne fallback relay log**. | Banc hermétique par construction ; capstone R4-F prérequis propre → débloqué. | `docs/plans/hermeticite-etage-l.md`, tom-transport 48/48, roles_charge build 139 |
| 2026-07-21 (question TTL) | **✅ VÉRIFIÉ : le TTL 24h du backup est ANCRÉ à l'émission d'origine et préservé à travers CHAQUE réplication (LOCKED #2 tient sous virus)** — question Malik : un backup qui obtient un message non livré à t+13h sait-il qu'il reste 11h (pas 24h de plus) ? Réponse par le code : PAS de « notification » d'auto-destruction ; le message PORTE son échéance ABSOLUE. `ReplicationPayload.expires_at` (u64, `backup/types.rs:164`) transporte l'expiry absolu ; à la réception, `store_replica` (`backup/store.rs:175`) fait `remaining_ttl = payload.expires_at.saturating_sub(now)` puis `BackupEntry::new(..., now, Some(remaining_ttl))` → `expires_at` final = l'expiry d'origine, JAMAIS ré-initialisé. Donc un backup à t+13h a `expires_at = t_origine+24h`, `remaining_ttl(now)=11h`, et l'auto-détruit à t_origine+24h via le tick de purge (`is_expired` = `now >= expires_at`). Garde bonus : `store_replica` REFUSE de stocker un message déjà expiré (`if now >= payload.expires_at return`). Conception élégante (rien à perdre vs une notification, pas d'horloge de notif) ; seule hypothèse = horloges à peu près synchronisées (timestamp Unix absolu ; le protocole a `ClockSkewMetrics` pour détecter un skew grossier). Conclusion : LOCKED #2 (24h max, purge globale sans exception) tient même sous réplication multi-hop — la vie du message ne se prolonge JAMAIS en changeant de gardien. | Confirmation positive d'une propriété LOCKED sous le mécanisme virus ; pas un bug, une vérification | `backup/store.rs:175`, `backup/types.rs:164` |
| 2026-07-21 après-midi (R4-F) | **✅ R4-F « DEUX INCONNUS SE TROUVENT » — PASS multi-host réel (baseline froide ~25 s, design ≤120 s).** Nouvelle sous-commande `tom-stress r4 --namespace <label>` (symétrique, process/hôte) : nœud hermétique (hermetic(), relais/mDNS/n0 désactivés, identité éphémère) + DHT Mainline P1 (préfixe tom-test- infalsifiable) + probe/echo applicatif nonce. Oracles : (a) RTT≤120s DUR, (b) source amorçage=rendez-vous (ProtocolEvent::DiscoveryTiming + pair gardé), (c) anti-squat compteur. Run Mac (2 process same-host, symétrique t+24,0 s, RTT 10ms). Run multi-host Mac↔NAS PASS : Mac t+23,5 s / NAS t+26,5 s, RTT ~20ms, 1 pair/0 rejet chacun, exit 0, zéro résiduel. **PREMIÈRE baseline froide mesurée** : design confirmé. NAS IP mouvementée (`.21`→`.83`, VM aarch64, relais public 82.67.95.8:3340 vivant). | Isolation R4 achevée ; design-first matérialisé ; capstone Phase C prérequis propre | `crates/tom-stress` r4 namespace, logs hermetic(), découverte 25s |
